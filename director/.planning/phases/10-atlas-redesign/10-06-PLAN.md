---
phase: 10-atlas-redesign
plan: 06
type: execute
wave: 4
depends_on: ["10-01"]
files_modified:
  - src/components/assets/AssetSetCard.tsx
  - src/components/assets/AssetItemGrid.tsx
  - src/components/assets/FileUploadZone.tsx
  - src/components/assets/GeneratedAssetsPanel.tsx
  - src/app/(auth)/projects/page.tsx
  - src/app/page.tsx
autonomous: true
requirements: [ATLAS-ASSETS-AND-REMAINING]

must_haves:
  truths:
    - "Asset components use purple accents, not orange"
    - "Projects page uses purple accents and atlas-dark surfaces"
    - "Landing page footer links hover purple, not orange"
    - "No file in the entire src/ directory contains #ff9064"
  artifacts:
    - path: "src/components/assets/AssetSetCard.tsx"
      provides: "Atlas-themed asset set card"
    - path: "src/components/assets/AssetItemGrid.tsx"
      provides: "Atlas-themed asset item grid"
    - path: "src/components/assets/FileUploadZone.tsx"
      provides: "Atlas-themed file upload zone"
    - path: "src/components/assets/GeneratedAssetsPanel.tsx"
      provides: "Atlas-themed generated assets panel"
    - path: "src/app/(auth)/projects/page.tsx"
      provides: "Atlas-themed projects page"
    - path: "src/app/page.tsx"
      provides: "Atlas-themed landing page"
  key_links: []
---

<objective>
Restyle all 4 asset components, the projects page, and the landing page. Then verify zero orange remains across the entire codebase.

Purpose: Final cleanup plan. After this, every visual file in the app uses the Atlas Cloud purple theme.
Output: All asset components, projects page, and landing page restyled. Full codebase audit confirms zero orange.
</objective>

<execution_context>
@/Users/aasish/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aasish/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-atlas-redesign/10-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle asset components</name>
  <read_first>src/components/assets/AssetSetCard.tsx, src/components/assets/AssetItemGrid.tsx, src/components/assets/FileUploadZone.tsx, src/components/assets/GeneratedAssetsPanel.tsx</read_first>
  <files>src/components/assets/AssetSetCard.tsx, src/components/assets/AssetItemGrid.tsx, src/components/assets/FileUploadZone.tsx, src/components/assets/GeneratedAssetsPanel.tsx</files>
  <action>
Apply across ALL FOUR files:

Color mapping:
- `#ff9064` -> `#7F72F7`
- `#ff7941` -> `#A78BFA`
- `#0e0e0e` -> `#0A0A0F`
- `#262626` -> `#12121A`
- `#333333` -> `#7F72F7]/10` in Tailwind or `rgba(127,114,247,0.1)` in inline styles
- `#131313` -> `#1A1A2E`
- `#adaaaa` -> `#A0A0B8`
- `#494847` -> `rgba(127,114,247,0.1)`

For AssetSetCard: Card backgrounds, hover borders, accent text.
For AssetItemGrid: Grid item backgrounds, selection highlights, borders.
For FileUploadZone: Upload zone border (dashed), hover state, accent icon color.
For GeneratedAssetsPanel: Panel backgrounds, provenance badge colors — keep amber for gap-fill badges (per Phase 08), but replace any orange accents.

NOTE: Keep amber (#FBBF24) for gap-fill provenance badges and purple for video-generation badges as established in Phase 08. Only replace the primary orange accent and old dark surfaces.

IMPORTANT: Do not change any logic or component structure. Only change color/style values.
  </action>
  <acceptance_criteria>
    - AssetSetCard.tsx does NOT contain #ff9064, #ff7941, #262626, #131313, #adaaaa
    - AssetItemGrid.tsx does NOT contain #ff9064, #0e0e0e, #262626, #131313, #adaaaa
    - FileUploadZone.tsx does NOT contain #ff9064, #ff7941, #adaaaa
    - GeneratedAssetsPanel.tsx does NOT contain #ff9064, #0e0e0e, #262626, #131313, #adaaaa
  </acceptance_criteria>
  <verify>
    <automated>grep -c "#ff9064\|#ff7941\|#0e0e0e\|#262626\|#131313\|#adaaaa" src/components/assets/AssetSetCard.tsx src/components/assets/AssetItemGrid.tsx src/components/assets/FileUploadZone.tsx src/components/assets/GeneratedAssetsPanel.tsx | grep -v ":0$" | wc -l | grep -q "^0$" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>All 4 asset components fully converted to Atlas Cloud theme</done>
</task>

<task type="auto">
  <name>Task 2: Restyle projects page, landing page, and run full codebase audit</name>
  <read_first>src/app/(auth)/projects/page.tsx, src/app/page.tsx</read_first>
  <files>src/app/(auth)/projects/page.tsx, src/app/page.tsx</files>
  <action>
Projects page — apply full color mapping (23 occurrences):
- `#ff9064` -> `#7F72F7`
- `#ff7941` -> `#A78BFA`
- `#262626` -> `#12121A`
- `#333333` -> `#7F72F7]/10` in Tailwind
- `#131313` -> `#1A1A2E`
- `#adaaaa` -> `#A0A0B8`
- `#494847` -> `rgba(127,114,247,0.1)` or `[#7F72F7]/10`
- `#1a1a1a` -> `#12121A`
- Selection checkboxes: `bg-[#ff9064] border-[#ff9064]` -> `bg-[#7F72F7] border-[#7F72F7]`
- Hover text: `group-hover:text-[#ff9064]` -> `group-hover:text-[#7F72F7]`
- Card hover shadow: `hover:shadow-[#ff9064]/5` -> `hover:shadow-[#7F72F7]/5`

Landing page (src/app/page.tsx) — apply color mapping (6 occurrences):
- Footer: `bg-[#0e0e0e]` -> `bg-[#0A0A0F]`
- Footer text: `text-[#adaaaa]` -> `text-[#A0A0B8]`
- Footer hover: `hover:text-[#ff9064]` -> `hover:text-[#7F72F7]`
- Footer border: `border-[#494847]/15` -> `border-[#7F72F7]/10`

After completing both files, run a FULL CODEBASE AUDIT:
```bash
grep -rn "#ff9064\|#ff7941\|#ff7939\|#ff5e07" src/ --include="*.tsx" --include="*.ts" --include="*.css"
```
If any matches are found in visual/UI files (not API routes), fix them. The two API route files (src/app/api/pipeline-nodes/generate-video/route.ts and src/app/api/pipeline-gaps/fill/route.ts) may contain orange hex in prompt strings for AI generation — these are acceptable to leave as-is since they are prompt content, not visual styling.

Also verify old background/surface colors are fully eliminated:
```bash
grep -rn "#0e0e0e\|#262626\|#131313" src/ --include="*.tsx" --include="*.css"
```

IMPORTANT: Do not change any logic or component structure. Only change color/style values.
  </action>
  <acceptance_criteria>
    - projects/page.tsx does NOT contain #ff9064, #ff7941, #262626, #131313, #adaaaa
    - page.tsx (landing) does NOT contain #ff9064, #0e0e0e, #adaaaa
    - Full grep of src/ for #ff9064 in *.tsx and *.css files returns 0 matches
    - Full grep of src/ for #0e0e0e in *.tsx and *.css files returns 0 matches
  </acceptance_criteria>
  <verify>
    <automated>grep -rc "#ff9064\|#0e0e0e\|#262626\|#131313\|#adaaaa" src/ --include="*.tsx" --include="*.css" | grep -v ":0$" | wc -l | grep -q "^0$" && echo "PASS: Zero old theme colors in tsx/css files" || echo "FAIL: Old theme colors remain"</automated>
  </verify>
  <done>Projects and landing pages restyled. Full codebase audit confirms zero orange/old-theme colors in any .tsx or .css file</done>
</task>

</tasks>

<verification>
After both tasks:
1. `grep -rn "#ff9064\|#ff7941\|#0e0e0e\|#262626\|#131313\|#adaaaa" src/ --include="*.tsx" --include="*.css"` returns zero matches
2. `npm run build` compiles without errors
3. `npm run test` passes (no visual assertion failures)
</verification>

<success_criteria>
- All asset components use purple accents and atlas-dark backgrounds
- Projects page fully restyled with purple accents
- Landing page footer uses purple hover and atlas background
- ZERO occurrences of #ff9064, #0e0e0e, #262626, #131313, or #adaaaa in any .tsx or .css file across the entire src/ directory
- The Atlas Cloud redesign is complete
</success_criteria>

<output>
After completion, create `.planning/phases/10-atlas-redesign/10-06-SUMMARY.md`
</output>
