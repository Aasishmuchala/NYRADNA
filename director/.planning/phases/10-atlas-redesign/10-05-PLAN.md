---
phase: 10-atlas-redesign
plan: 05
type: execute
wave: 4
depends_on: ["10-01"]
files_modified:
  - src/components/pipeline/SegmentNode.tsx
  - src/components/pipeline/PipelineCanvas.tsx
  - src/components/pipeline/GapMarker.tsx
  - src/components/pipeline/GapDetailPanel.tsx
  - src/components/pipeline/NodeControls.tsx
  - src/components/pipeline/ValidationReportPanel.tsx
autonomous: true
requirements: [ATLAS-PIPELINE-COMPONENTS]

must_haves:
  truths:
    - "SegmentNode uses purple accents and atlas-dark backgrounds"
    - "GapMarker uses purple tones instead of orange"
    - "GapDetailPanel buttons and accents are purple"
    - "NodeControls uses atlas-dark surfaces and purple accents"
    - "ValidationReportPanel uses atlas-dark surfaces and purple accents"
    - "PipelineCanvas background matches atlas theme"
  artifacts:
    - path: "src/components/pipeline/SegmentNode.tsx"
      provides: "Atlas-themed segment node"
    - path: "src/components/pipeline/PipelineCanvas.tsx"
      provides: "Atlas-themed pipeline canvas"
    - path: "src/components/pipeline/GapMarker.tsx"
      provides: "Atlas-themed gap marker"
    - path: "src/components/pipeline/GapDetailPanel.tsx"
      provides: "Atlas-themed gap detail panel"
    - path: "src/components/pipeline/NodeControls.tsx"
      provides: "Atlas-themed node controls"
    - path: "src/components/pipeline/ValidationReportPanel.tsx"
      provides: "Atlas-themed validation report panel"
  key_links: []
---

<objective>
Restyle all 6 pipeline components from orange to purple Atlas Cloud theme.

Purpose: Pipeline components (SegmentNode, PipelineCanvas, GapMarker, GapDetailPanel, NodeControls, ValidationReportPanel) have 28 hardcoded old-theme color occurrences. Batch-replace them.
Output: All pipeline components use purple accents, atlas-dark backgrounds.
</objective>

<execution_context>
@/Users/aasish/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aasish/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-atlas-redesign/10-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle SegmentNode, PipelineCanvas, and GapMarker</name>
  <read_first>src/components/pipeline/SegmentNode.tsx, src/components/pipeline/PipelineCanvas.tsx, src/components/pipeline/GapMarker.tsx</read_first>
  <files>src/components/pipeline/SegmentNode.tsx, src/components/pipeline/PipelineCanvas.tsx, src/components/pipeline/GapMarker.tsx</files>
  <action>
Apply across ALL THREE files:

Color mapping:
- `#ff9064` -> `#7F72F7`
- `#ff7941` -> `#A78BFA`
- `#262626` -> `#12121A`
- `#333333` -> `#7F72F7]/10` in Tailwind classes or `rgba(127,114,247,0.1)` in inline styles
- `#131313` -> `#1A1A2E`
- `#adaaaa` -> `#A0A0B8`
- `#494847` -> `rgba(127,114,247,0.1)`

For SegmentNode: Node borders, status indicators, selection highlights — all orange to purple.
For PipelineCanvas: Background color, edge colors, any canvas-level styling.
For GapMarker: Marker badge backgrounds, edge label styling — orange to purple.

NOTE: Keep severity-based colors intact (success=#34D399, warning=#FBBF24, error=#EF4444). Only replace the orange accent (#ff9064) and old background/surface colors.
NOTE: Keep the amber color for gap-fill provenance badges (per Phase 08 decision). Only replace orange that is used as the primary accent.

IMPORTANT: Do not change any logic or component structure. Only change color/style values.
  </action>
  <acceptance_criteria>
    - SegmentNode.tsx does NOT contain #ff9064, #ff7941, #262626, #131313, #adaaaa
    - PipelineCanvas.tsx background uses atlas-dark colors
    - GapMarker.tsx does NOT contain #ff9064, #adaaaa
  </acceptance_criteria>
  <verify>
    <automated>grep -c "#ff9064\|#ff7941\|#262626\|#131313\|#adaaaa" src/components/pipeline/SegmentNode.tsx src/components/pipeline/PipelineCanvas.tsx src/components/pipeline/GapMarker.tsx | grep -v ":0$" | wc -l | grep -q "^0$" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>SegmentNode, PipelineCanvas, and GapMarker fully converted to Atlas Cloud theme</done>
</task>

<task type="auto">
  <name>Task 2: Restyle GapDetailPanel, NodeControls, and ValidationReportPanel</name>
  <read_first>src/components/pipeline/GapDetailPanel.tsx, src/components/pipeline/NodeControls.tsx, src/components/pipeline/ValidationReportPanel.tsx</read_first>
  <files>src/components/pipeline/GapDetailPanel.tsx, src/components/pipeline/NodeControls.tsx, src/components/pipeline/ValidationReportPanel.tsx</files>
  <action>
Apply across ALL THREE files:

Color mapping:
- `#ff9064` -> `#7F72F7`
- `#ff7941` -> `#A78BFA`
- `#262626` -> `#12121A`
- `#333333` -> `#7F72F7]/10` in Tailwind or `rgba(127,114,247,0.1)` in inline styles
- `#131313` -> `#1A1A2E`
- `#adaaaa` -> `#A0A0B8`
- `#494847` -> `rgba(127,114,247,0.1)`

For GapDetailPanel: Button colors, panel backgrounds, accent text.
For NodeControls: Control button colors, hover states, borders.
For ValidationReportPanel: Report card backgrounds, severity badges keep their colors (red/yellow/green), but panel chrome and accent colors change to purple.

NOTE: Keep severity colors (success=#34D399, warning=#FBBF24, error=#EF4444) untouched. Only replace the primary orange accent and old dark theme surface colors.

IMPORTANT: Do not change any logic or component structure. Only change color/style values.
  </action>
  <acceptance_criteria>
    - GapDetailPanel.tsx does NOT contain #ff9064, #ff7941, #262626, #131313, #adaaaa
    - NodeControls.tsx does NOT contain #ff9064, #ff7941, #262626, #131313, #adaaaa
    - ValidationReportPanel.tsx does NOT contain #ff9064, #ff7941, #262626, #131313, #adaaaa
  </acceptance_criteria>
  <verify>
    <automated>grep -c "#ff9064\|#ff7941\|#262626\|#131313\|#adaaaa" src/components/pipeline/GapDetailPanel.tsx src/components/pipeline/NodeControls.tsx src/components/pipeline/ValidationReportPanel.tsx | grep -v ":0$" | wc -l | grep -q "^0$" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>GapDetailPanel, NodeControls, and ValidationReportPanel fully converted to Atlas Cloud theme</done>
</task>

</tasks>

<verification>
After both tasks:
1. `grep -rn "#ff9064\|#ff7941\|#262626\|#131313\|#adaaaa" src/components/pipeline/*.tsx` returns zero matches
2. `npm run build` compiles without errors
</verification>

<success_criteria>
- All 6 pipeline components use purple accents and atlas-dark backgrounds
- Severity colors (red, yellow, green) remain unchanged
- No orange hex codes remain in any pipeline component
</success_criteria>

<output>
After completion, create `.planning/phases/10-atlas-redesign/10-05-SUMMARY.md`
</output>
