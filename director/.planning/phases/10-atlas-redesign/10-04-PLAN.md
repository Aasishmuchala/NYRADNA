---
phase: 10-atlas-redesign
plan: 04
type: execute
wave: 3
depends_on: ["10-01"]
files_modified:
  - src/app/(auth)/create/review/page.tsx
  - src/app/(auth)/create/generating/page.tsx
  - src/app/(auth)/create/export/page.tsx
  - src/app/(auth)/create/asset-sets/page.tsx
  - src/app/(auth)/create/intent/page.tsx
autonomous: true
requirements: [ATLAS-CREATE-PAGES-B]

must_haves:
  truths:
    - "Review page shows purple accents, not orange"
    - "Generating page shows purple accents, not orange"
    - "Export page shows purple accents, not orange"
    - "Asset Sets page shows purple accents, not orange"
    - "Intent page shadow glows use purple, not orange"
  artifacts:
    - path: "src/app/(auth)/create/review/page.tsx"
      provides: "Atlas-themed review page"
    - path: "src/app/(auth)/create/generating/page.tsx"
      provides: "Atlas-themed generating page"
    - path: "src/app/(auth)/create/export/page.tsx"
      provides: "Atlas-themed export page"
    - path: "src/app/(auth)/create/asset-sets/page.tsx"
      provides: "Atlas-themed asset sets page"
    - path: "src/app/(auth)/create/intent/page.tsx"
      provides: "Atlas-themed intent page"
  key_links: []
---

<objective>
Restyle the remaining 5 create/* workflow pages from orange to purple Atlas Cloud theme.

Purpose: Completes the create/* workflow restyling. These pages have fewer hardcoded values (~26 occurrences combined across review, generating, export, asset-sets; intent uses mostly CSS variables but has rgba shadows to fix).
Output: All remaining create pages use purple accents, atlas-dark backgrounds.
</objective>

<execution_context>
@/Users/aasish/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aasish/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-atlas-redesign/10-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle review, generating, and export pages</name>
  <read_first>src/app/(auth)/create/review/page.tsx, src/app/(auth)/create/generating/page.tsx, src/app/(auth)/create/export/page.tsx</read_first>
  <files>src/app/(auth)/create/review/page.tsx, src/app/(auth)/create/generating/page.tsx, src/app/(auth)/create/export/page.tsx</files>
  <action>
Apply these replacements across ALL THREE files:

Color mapping:
- `#ff9064` -> `#7F72F7`
- `#ff7941` -> `#A78BFA`
- `#0e0e0e` -> `#0A0A0F`
- `#262626` -> `#12121A`
- `#333333` -> Tailwind `[#7F72F7]/10` or `rgba(127,114,247,0.1)`
- `#131313` -> `#1A1A2E`
- `#adaaaa` -> `#A0A0B8`
- `#494847` -> `rgba(127,114,247,0.1)` or equivalent

For rgba orange values: `rgba(255, 144, 100, X)` -> `rgba(127, 114, 247, X)` (keep alpha).
For button gradients: `from-[#ff9064] to-[#ff7941]` -> `from-[#7F72F7] to-[#A78BFA]`.
For `text-[#571a00]` on CTA buttons -> `text-white`.
For font-manrope -> remove (font-headline resolves to Inter).

Export page specific: The progress bar shadow `shadow-[0_0_12px_rgba(255,144,100,0.5)]` -> `shadow-[0_0_12px_rgba(127,114,247,0.5)]`.

IMPORTANT: Do not change any logic, state management, or component structure. Only change color/style values.
  </action>
  <acceptance_criteria>
    - review/page.tsx does NOT contain #ff9064, #ff7941, #262626, #131313, #adaaaa
    - generating/page.tsx does NOT contain #ff9064, #ff7941, #262626, #131313, #adaaaa
    - export/page.tsx does NOT contain rgba(255, 144, 100
  </acceptance_criteria>
  <verify>
    <automated>grep -c "#ff9064\|#ff7941\|#262626\|#131313\|#adaaaa\|font-manrope" "src/app/(auth)/create/review/page.tsx" "src/app/(auth)/create/generating/page.tsx" "src/app/(auth)/create/export/page.tsx" | grep -v ":0$" | wc -l | grep -q "^0$" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Review, Generating, and Export pages fully converted to Atlas Cloud purple theme</done>
</task>

<task type="auto">
  <name>Task 2: Restyle asset-sets and intent pages</name>
  <read_first>src/app/(auth)/create/asset-sets/page.tsx, src/app/(auth)/create/intent/page.tsx</read_first>
  <files>src/app/(auth)/create/asset-sets/page.tsx, src/app/(auth)/create/intent/page.tsx</files>
  <action>
Asset Sets page — apply same color mapping:
- `#ff9064` -> `#7F72F7`
- `#ff7941` -> `#A78BFA`
- `#262626` -> `#12121A`
- `#333333` -> Tailwind `[#7F72F7]/10`
- `#131313` -> `#1A1A2E`
- `#adaaaa` -> `#A0A0B8`
- Button gradients: `from-[#ff9064] to-[#ff7941]` -> `from-[#7F72F7] to-[#A78BFA]`
- `text-[#571a00]` -> `text-white`

Intent page — this page mostly uses CSS variables (which are already updated by Plan 01). But fix:
- `rgba(255,144,100,0.2)` in shadow -> `rgba(127,114,247,0.2)`
- `rgba(255,144,100,0.3)` in shadow -> `rgba(127,114,247,0.3)`
- Any remaining hardcoded orange hex values

IMPORTANT: Do not change any logic, state management, or component structure. Only change color/style values.
  </action>
  <acceptance_criteria>
    - asset-sets/page.tsx does NOT contain #ff9064, #ff7941, #262626, #131313, #adaaaa
    - intent/page.tsx does NOT contain rgba(255, 144, 100
    - Both files contain #7F72F7 or use CSS variable references that resolve to purple
  </acceptance_criteria>
  <verify>
    <automated>grep -c "#ff9064\|#ff7941\|#262626\|#131313\|#adaaaa" "src/app/(auth)/create/asset-sets/page.tsx" | grep -q "^0$" && grep -c "rgba(255, 144, 100\|rgba(255,144,100" "src/app/(auth)/create/intent/page.tsx" | grep -q "^0$" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Asset Sets and Intent pages fully converted to Atlas Cloud purple theme</done>
</task>

</tasks>

<verification>
After both tasks:
1. `grep -rn "#ff9064\|#ff7941\|#262626\|#131313\|#adaaaa" src/app/\(auth\)/create/review/page.tsx src/app/\(auth\)/create/generating/page.tsx src/app/\(auth\)/create/export/page.tsx src/app/\(auth\)/create/asset-sets/page.tsx src/app/\(auth\)/create/intent/page.tsx` returns zero matches
2. `npm run build` compiles without errors
</verification>

<success_criteria>
- All 5 remaining create pages use purple Atlas Cloud accents
- No orange hex codes or orange rgba values remain in any create/* page
- All 10 create/* pages are now fully restyled
</success_criteria>

<output>
After completion, create `.planning/phases/10-atlas-redesign/10-04-SUMMARY.md`
</output>
