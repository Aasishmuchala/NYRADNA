---
phase: 10-atlas-redesign
plan: 03
type: execute
wave: 3
depends_on: ["10-01"]
files_modified:
  - src/app/(auth)/create/directors-cut/page.tsx
  - src/app/(auth)/create/brief/page.tsx
  - src/app/(auth)/create/style-dna/page.tsx
  - src/app/(auth)/create/character-setup/page.tsx
  - src/app/(auth)/create/pipeline/page.tsx
autonomous: true
requirements: [ATLAS-CREATE-PAGES-A]

must_haves:
  truths:
    - "Directors Cut page shows purple accents, not orange"
    - "Brief page shows purple accents, not orange"
    - "Style DNA page uses atlas backgrounds"
    - "Character Setup page uses atlas backgrounds and purple accents"
    - "Pipeline page uses atlas backgrounds and purple accents"
  artifacts:
    - path: "src/app/(auth)/create/directors-cut/page.tsx"
      provides: "Atlas-themed directors cut page"
    - path: "src/app/(auth)/create/brief/page.tsx"
      provides: "Atlas-themed brief page"
    - path: "src/app/(auth)/create/style-dna/page.tsx"
      provides: "Atlas-themed style DNA page"
    - path: "src/app/(auth)/create/character-setup/page.tsx"
      provides: "Atlas-themed character setup page"
    - path: "src/app/(auth)/create/pipeline/page.tsx"
      provides: "Atlas-themed pipeline page"
  key_links: []
---

<objective>
Restyle the 5 heaviest create/* workflow pages from orange to purple Atlas Cloud theme.

Purpose: These 5 pages (directors-cut, brief, style-dna, character-setup, pipeline) contain the most hardcoded orange/old-theme values (~133 occurrences combined). Batch-replacing them in one plan.
Output: All 5 pages use purple accents, atlas-dark backgrounds, no orange remnants.
</objective>

<execution_context>
@/Users/aasish/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aasish/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-atlas-redesign/10-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle directors-cut and brief pages</name>
  <read_first>src/app/(auth)/create/directors-cut/page.tsx, src/app/(auth)/create/brief/page.tsx</read_first>
  <files>src/app/(auth)/create/directors-cut/page.tsx, src/app/(auth)/create/brief/page.tsx</files>
  <action>
Apply these replacements across BOTH files. Search and replace every occurrence:

Color mapping (apply to ALL occurrences in both files):
- `#ff9064` -> `#7F72F7` (primary accent)
- `#ff7941` -> `#A78BFA` (primary light variant)
- `#0e0e0e` -> `#0A0A0F` (base background)
- `#262626` -> `#12121A` (surface/card bg)
- `#333333` -> `rgba(127,114,247,0.1)` or `#7F72F7/10` in Tailwind (borders)
- `#131313` -> `#1A1A2E` (elevated surface)
- `#adaaaa` -> `#A0A0B8` (secondary text)
- `#494847` -> `rgba(127,114,247,0.1)` (subtle borders)

For rgba values:
- `rgba(255, 144, 100, ...)` -> `rgba(127, 114, 247, ...)` (keep same alpha)
- `rgba(255, 121, 65, ...)` -> `rgba(167, 139, 250, ...)` (keep same alpha)

Font references:
- `font-manrope` -> remove (Inter is now inherited via CSS variable --font-headline)
- `font-headline` stays (it now resolves to Inter via globals.css)

Button gradients:
- `from-[#ff9064] to-[#ff7941]` -> `from-[#7F72F7] to-[#A78BFA]`
- `text-[#571a00]` on buttons -> `text-white`

Shadows with orange glow:
- `shadow-[0_10px_30px_rgba(255,144,100,0.2)]` -> `shadow-[0_10px_30px_rgba(127,114,247,0.2)]`
- Similar pattern for all orange shadow values

IMPORTANT: Do not change any logic, state management, or component structure. Only change color/style values.
  </action>
  <acceptance_criteria>
    - directors-cut/page.tsx does NOT contain #ff9064, #ff7941, #0e0e0e, #262626, #131313, #adaaaa, font-manrope
    - brief/page.tsx does NOT contain #ff9064, #ff7941, #0e0e0e, #262626, #131313, #adaaaa, font-manrope
    - Both files contain #7F72F7
  </acceptance_criteria>
  <verify>
    <automated>grep -c "#ff9064\|#ff7941\|#0e0e0e\|#262626\|#131313\|#adaaaa\|font-manrope" "src/app/(auth)/create/directors-cut/page.tsx" "src/app/(auth)/create/brief/page.tsx" | grep -v ":0$" | wc -l | grep -q "^0$" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Directors Cut and Brief pages fully converted to Atlas Cloud purple theme</done>
</task>

<task type="auto">
  <name>Task 2: Restyle style-dna, character-setup, and pipeline pages</name>
  <read_first>src/app/(auth)/create/style-dna/page.tsx, src/app/(auth)/create/character-setup/page.tsx, src/app/(auth)/create/pipeline/page.tsx</read_first>
  <files>src/app/(auth)/create/style-dna/page.tsx, src/app/(auth)/create/character-setup/page.tsx, src/app/(auth)/create/pipeline/page.tsx</files>
  <action>
Apply the SAME color mapping as Task 1 across ALL THREE files:

Color mapping:
- `#ff9064` -> `#7F72F7`
- `#ff7941` -> `#A78BFA`
- `#0e0e0e` -> `#0A0A0F`
- `#262626` -> `#12121A`
- `#333333` -> `#7F72F7]/10` (as Tailwind border class)
- `#131313` -> `#1A1A2E`
- `#adaaaa` -> `#A0A0B8`
- `#494847` -> `rgba(127,114,247,0.1)` or equivalent Tailwind

For rgba orange values -> rgba purple values (keep alpha).
For button gradients: `from-[#ff9064] to-[#ff7941]` -> `from-[#7F72F7] to-[#A78BFA]`.
For font-manrope -> remove the class (font-headline now resolves to Inter).
For `text-[#571a00]` on CTA buttons -> `text-white`.

Pipeline page specific: The "Generate Videos" button was already purple-themed per Phase 07 decision. Verify it stays consistent (should already use purple, but if any orange crept in, replace it).

IMPORTANT: Do not change any logic, state management, or component structure. Only change color/style values.
  </action>
  <acceptance_criteria>
    - style-dna/page.tsx does NOT contain #ff9064, #ff7941, #262626, #131313, #adaaaa
    - character-setup/page.tsx does NOT contain #ff9064, #ff7941, #262626, #131313, #adaaaa
    - pipeline/page.tsx does NOT contain #ff9064, #ff7941, #262626, #131313, #adaaaa
    - All three files contain #7F72F7 where accents are used
  </acceptance_criteria>
  <verify>
    <automated>grep -c "#ff9064\|#ff7941\|#262626\|#131313\|#adaaaa\|font-manrope" "src/app/(auth)/create/style-dna/page.tsx" "src/app/(auth)/create/character-setup/page.tsx" "src/app/(auth)/create/pipeline/page.tsx" | grep -v ":0$" | wc -l | grep -q "^0$" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Style DNA, Character Setup, and Pipeline pages fully converted to Atlas Cloud purple theme</done>
</task>

</tasks>

<verification>
After both tasks:
1. `grep -rn "#ff9064\|#ff7941\|#262626\|#131313\|#adaaaa\|font-manrope" src/app/\(auth\)/create/directors-cut/page.tsx src/app/\(auth\)/create/brief/page.tsx src/app/\(auth\)/create/style-dna/page.tsx src/app/\(auth\)/create/character-setup/page.tsx src/app/\(auth\)/create/pipeline/page.tsx` returns zero matches
2. `npm run build` compiles without errors
</verification>

<success_criteria>
- All 5 pages use #7F72F7 purple accents instead of #ff9064 orange
- All backgrounds use atlas-dark palette (#0A0A0F, #12121A, #1A1A2E)
- No Manrope font references remain
- No orange hex codes or orange rgba values remain
</success_criteria>

<output>
After completion, create `.planning/phases/10-atlas-redesign/10-03-SUMMARY.md`
</output>
