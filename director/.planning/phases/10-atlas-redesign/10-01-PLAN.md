---
phase: 10-atlas-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/globals.css
  - src/app/layout.tsx
  - src/app/(auth)/layout.tsx
autonomous: true
requirements: [ATLAS-FOUNDATION]

must_haves:
  truths:
    - "CSS variables resolve to purple/atlas palette values, not orange"
    - "Root layout uses Inter font (not Manrope) for headline variable"
    - "Body background is #0A0A0F, not #0e0e0e"
    - "All utility classes (.glass-card, .text-gradient-primary, .cinematic-glow-warm) use purple"
  artifacts:
    - path: "src/app/globals.css"
      provides: "Atlas Cloud CSS variables and utility classes"
      contains: "--color-primary: #7F72F7"
    - path: "src/app/layout.tsx"
      provides: "Inter font import and body bg"
      contains: "bg-[#0A0A0F]"
    - path: "src/app/(auth)/layout.tsx"
      provides: "Auth wrapper with atlas bg"
      contains: "bg-[#0A0A0F]"
  key_links:
    - from: "src/app/globals.css"
      to: "all pages using CSS variables"
      via: "@theme CSS variable definitions"
      pattern: "--color-primary: #7F72F7"
---

<objective>
Replace the orange Obsidian Slate theme foundation with the Atlas Cloud purple theme across globals.css, root layout, and auth layout.

Purpose: Every page inherits from these 3 files. Changing them first propagates purple to all pages using CSS variables (dashboard, settings, characters, intent, etc.) without touching those files.
Output: Updated CSS variables, Inter font, purple utility classes, dark backgrounds.
</objective>

<execution_context>
@/Users/aasish/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aasish/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-atlas-redesign/10-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace CSS variables and utility classes in globals.css</name>
  <read_first>src/app/globals.css</read_first>
  <files>src/app/globals.css</files>
  <action>
Replace the entire @theme block with Atlas Cloud palette:

```
@theme {
  /* Colors - Atlas Cloud Purple */
  --color-primary: #7F72F7;
  --color-primary-container: #6B5CF7;
  --color-primary-dim: #6B5CF7;
  --color-primary-fixed: #6B5CF7;
  --color-primary-fixed-dim: #5B4CD9;
  --color-on-primary: #FFFFFF;
  --color-on-primary-container: #1A1A2E;
  --color-on-primary-fixed: #FFFFFF;

  /* Secondary */
  --color-secondary: #A78BFA;
  --color-secondary-container: #2D2654;
  --color-on-secondary: #1A1A2E;
  --color-on-secondary-container: #DDD6FE;

  /* Tertiary (keep distinct accent) */
  --color-tertiary: #81e9ff;
  --color-tertiary-container: #004d5a;
  --color-tertiary-dim: #00d1ee;
  --color-on-tertiary-container: #c0f0ff;
  --color-on-tertiary-fixed: #001f26;

  /* Error */
  --color-error: #EF4444;
  --color-error-container: #93000a;
  --color-error-dim: #DC2626;
  --color-on-error: #FFFFFF;
  --color-on-error-container: #ffdad6;

  /* Surface (Atlas Ultra-Dark) */
  --color-surface: #0A0A0F;
  --color-surface-bright: #2A2A3E;
  --color-surface-container: #1A1A2E;
  --color-surface-container-high: #1A1A2E;
  --color-surface-container-highest: #1A1A2E;
  --color-surface-container-low: #12121A;
  --color-surface-container-lowest: #0A0A0F;
  --color-on-surface: #FFFFFF;
  --color-on-surface-variant: #A0A0B8;

  /* Outline */
  --color-outline-variant: rgba(127, 114, 247, 0.1);

  /* Fonts */
  --font-headline: var(--font-inter), 'Inter', sans-serif;
  --font-body: var(--font-inter), 'Inter', sans-serif;
  --font-label: var(--font-inter), 'Inter', sans-serif;
}
```

Update utility classes:
- `.glass-card` and `.glass-panel` and `.glass-input`: change `rgba(38, 38, 38, 0.4)` to `rgba(18, 18, 26, 0.8)` and add `backdrop-filter: blur(20px)` (increase blur for glassmorphism)
- `.cinematic-glow-warm`: change `rgba(255, 144, 100, 0.1)` to `rgba(127, 114, 247, 0.15)`
- `.hero-vignette`: change `rgba(14, 14, 14, 0.8)` to `rgba(10, 10, 15, 0.8)`
- `.text-gradient-primary`: change `#ff9064, #ff7941` to `#7F72F7, #A78BFA`
- `.headline-font`: change `'Manrope'` to `'Inter'`
- `.custom-scrollbar` thumb: change `#262626` to `rgba(127, 114, 247, 0.15)`

Do NOT change the shimmer animation classes or Material Symbols base styles.
  </action>
  <acceptance_criteria>
    - File does NOT contain #ff9064, #ff7941, #ff7939, #ff5e07, #571a00, #431200
    - File does NOT contain #0e0e0e
    - File does NOT contain rgba(38, 38, 38
    - File does NOT contain rgba(255, 144, 100
    - File does NOT contain rgba(14, 14, 14
    - File does NOT contain 'Manrope'
    - File DOES contain --color-primary: #7F72F7
    - File DOES contain --color-surface: #0A0A0F
  </acceptance_criteria>
  <verify>
    <automated>grep -c "#ff9064\|#ff7941\|#0e0e0e\|Manrope" src/app/globals.css | grep -q "^0$" && grep -q "#7F72F7" src/app/globals.css && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>globals.css uses Atlas Cloud purple palette, all utility classes updated, no orange remnants</done>
</task>

<task type="auto">
  <name>Task 2: Update root layout and auth layout fonts and backgrounds</name>
  <read_first>src/app/layout.tsx, src/app/(auth)/layout.tsx</read_first>
  <files>src/app/layout.tsx, src/app/(auth)/layout.tsx</files>
  <action>
In `src/app/layout.tsx`:
1. Remove the Manrope import. Keep Inter import. Update Inter weights to include '700' and '800' (for headings): `weight: ['400', '500', '600', '700', '800']`
2. Remove `const manrope = Manrope(...)` block entirely
3. Change body className from `${manrope.variable} ${inter.variable} font-sans bg-[#0e0e0e]` to `${inter.variable} font-sans bg-[#0A0A0F]`
4. Keep everything else (Material Symbols link, metadata, dark class)

In `src/app/(auth)/layout.tsx`:
1. Change `bg-[#0e0e0e]` to `bg-[#0A0A0F]`
  </action>
  <acceptance_criteria>
    - layout.tsx does NOT contain "Manrope" or "manrope" (case-insensitive)
    - layout.tsx does NOT contain #0e0e0e
    - layout.tsx DOES contain bg-[#0A0A0F]
    - layout.tsx DOES contain weight: ['400', '500', '600', '700', '800']
    - (auth)/layout.tsx does NOT contain #0e0e0e
    - (auth)/layout.tsx DOES contain bg-[#0A0A0F]
  </acceptance_criteria>
  <verify>
    <automated>grep -ci "manrope\|#0e0e0e" src/app/layout.tsx | grep -q "^0$" && grep -q "#0A0A0F" src/app/layout.tsx && grep -q "#0A0A0F" "src/app/(auth)/layout.tsx" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Root layout uses Inter only (no Manrope), both layouts use #0A0A0F background</done>
</task>

</tasks>

<verification>
After both tasks:
1. `grep -rn "#ff9064\|#0e0e0e\|Manrope" src/app/globals.css src/app/layout.tsx "src/app/(auth)/layout.tsx"` returns zero matches
2. `grep -c "#7F72F7" src/app/globals.css` returns at least 1
3. `npm run build` compiles without errors (font and CSS changes valid)
</verification>

<success_criteria>
- All CSS variables point to Atlas Cloud purple palette
- Inter is the only font (Manrope completely removed from foundation)
- Body backgrounds are #0A0A0F across root and auth layouts
- All utility classes use purple tones instead of orange
- No orange hex values (#ff9064, #ff7941, etc.) in any of the 3 files
</success_criteria>

<output>
After completion, create `.planning/phases/10-atlas-redesign/10-01-SUMMARY.md`
</output>
