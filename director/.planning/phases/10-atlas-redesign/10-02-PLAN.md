---
phase: 10-atlas-redesign
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - src/components/layout/Sidebar.tsx
  - src/components/layout/TopNav.tsx
autonomous: true
requirements: [ATLAS-LAYOUT]

must_haves:
  truths:
    - "Sidebar active state shows purple text and left border, not orange"
    - "Sidebar brand logo has purple background, not orange"
    - "New Project CTA uses purple gradient, not orange"
    - "TopNav search focus ring is purple, not orange"
    - "TopNav notification dot is purple, not orange"
    - "All muted text uses #A0A0B8 instead of #adaaaa"
  artifacts:
    - path: "src/components/layout/Sidebar.tsx"
      provides: "Atlas-themed sidebar with purple accents"
      contains: "#7F72F7"
    - path: "src/components/layout/TopNav.tsx"
      provides: "Atlas-themed top navigation"
      contains: "#7F72F7"
  key_links:
    - from: "src/components/layout/Sidebar.tsx"
      to: "all authenticated pages"
      via: "AuthLayout imports Sidebar"
      pattern: "bg-\\[#0A0A0F\\]"
    - from: "src/components/layout/TopNav.tsx"
      to: "all authenticated pages"
      via: "AuthLayout imports TopNav"
      pattern: "border-\\[#7F72F7\\]"
---

<objective>
Restyle Sidebar and TopNav from orange Obsidian Slate to purple Atlas Cloud aesthetic.

Purpose: These two components frame every authenticated page. Updating them gives the entire app a purple shell before touching individual page content.
Output: Purple-accented sidebar with glassmorphism, purple-accented top nav.
</objective>

<execution_context>
@/Users/aasish/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aasish/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-atlas-redesign/10-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle Sidebar.tsx to Atlas Cloud theme</name>
  <read_first>src/components/layout/Sidebar.tsx</read_first>
  <files>src/components/layout/Sidebar.tsx</files>
  <action>
Perform these exact color replacements throughout the entire file:

1. Background: `bg-[#0e0e0e]` -> `bg-[#0A0A0F]`
2. Active backgrounds: `bg-[#262626]` -> `bg-[#7F72F7]/5` (subtle purple tint)
3. Active text: `text-[#ff9064]` -> `text-[#7F72F7]`
4. Muted text: `text-[#adaaaa]` -> `text-[#A0A0B8]`
5. Hover backgrounds: `hover:bg-[#131313]` -> `hover:bg-[#1A1A2E]`
6. Border colors: `border-[#262626]` -> `border-[#7F72F7]/10`
7. Font class: `font-manrope` -> `font-bold tracking-tight` (Inter is now the headline font via CSS variable)

Brand section logo:
- Change `bg-[#ff9064]` to `bg-gradient-to-br from-[#7F72F7] to-[#A78BFA]`
- Change `text-[#571a00]` to `text-white`

New Project CTA button:
- Change `from-[#ff9064] to-[#ff7941]` to `from-[#7F72F7] to-[#A78BFA]`
- Change `text-[#571a00]` to `text-white`

Active sidebar item pattern — add left border indicator:
For active nav items and active workflow steps, add `border-l-2 border-[#7F72F7]` to the active className (alongside the existing `bg-[#7F72F7]/5 text-[#7F72F7]` replacement).

Workflow section label: `text-[#adaaaa]` -> `text-[#6B6B80]` (use muted for section headers per design spec).

Blocked step text: `text-[#555]` -> `text-[#6B6B80]`
  </action>
  <acceptance_criteria>
    - File does NOT contain #ff9064 or #ff7941 or #ff7939
    - File does NOT contain #0e0e0e
    - File does NOT contain #262626
    - File does NOT contain #131313
    - File does NOT contain #adaaaa
    - File does NOT contain #571a00
    - File does NOT contain font-manrope
    - File DOES contain #7F72F7
    - File DOES contain #A78BFA
    - File DOES contain #0A0A0F
    - File DOES contain #1A1A2E
    - File DOES contain #A0A0B8
  </acceptance_criteria>
  <verify>
    <automated>grep -c "#ff9064\|#ff7941\|#0e0e0e\|#262626\|#131313\|#adaaaa\|font-manrope" src/components/layout/Sidebar.tsx | grep -q "^0$" && grep -q "#7F72F7" src/components/layout/Sidebar.tsx && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Sidebar uses purple accents, atlas backgrounds, Inter font, no orange colors remain</done>
</task>

<task type="auto">
  <name>Task 2: Restyle TopNav.tsx to Atlas Cloud theme</name>
  <read_first>src/components/layout/TopNav.tsx</read_first>
  <files>src/components/layout/TopNav.tsx</files>
  <action>
Perform these exact color replacements throughout the entire file:

1. Nav background: `bg-[#0e0e0e]/70` -> `bg-[#0A0A0F]/70`
2. Nav border: `border-[#262626]` -> `border-[#7F72F7]/10`
3. Search input bg: `bg-[#1a1a1a]` -> `bg-[#12121A]`
4. Search placeholder: `placeholder-[#adaaaa]` -> `placeholder-[#6B6B80]`
5. Search ring: `ring-[#262626]` -> `ring-[#7F72F7]/10`
6. Search focus ring: `focus:ring-[#ff9064]/50` -> `focus:ring-[#7F72F7]/50`
7. Icon muted color: `text-[#adaaaa]` -> `text-[#A0A0B8]`
8. Notification dot: `bg-[#ff9064]` -> `bg-[#7F72F7]`
9. Hover backgrounds: `hover:bg-[#262626]` -> `hover:bg-[#1A1A2E]`
10. Avatar ring: `bg-[#262626]` -> `bg-[#1A1A2E]`
11. Avatar ring border: `ring-[#3a3a3a]` -> `ring-[#7F72F7]/20`
12. Subtitle text: `text-[#adaaaa]` -> `text-[#A0A0B8]`
  </action>
  <acceptance_criteria>
    - File does NOT contain #ff9064
    - File does NOT contain #0e0e0e
    - File does NOT contain #262626
    - File does NOT contain #adaaaa
    - File does NOT contain #3a3a3a
    - File DOES contain #7F72F7
    - File DOES contain #0A0A0F
    - File DOES contain #12121A
    - File DOES contain #A0A0B8
  </acceptance_criteria>
  <verify>
    <automated>grep -c "#ff9064\|#0e0e0e\|#262626\|#adaaaa\|#3a3a3a" src/components/layout/TopNav.tsx | grep -q "^0$" && grep -q "#7F72F7" src/components/layout/TopNav.tsx && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>TopNav uses purple accents, atlas backgrounds, no orange colors remain</done>
</task>

</tasks>

<verification>
After both tasks:
1. `grep -rn "#ff9064\|#0e0e0e\|#262626\|#131313\|#adaaaa\|font-manrope" src/components/layout/Sidebar.tsx src/components/layout/TopNav.tsx` returns zero matches
2. `npm run build` compiles without errors
</verification>

<success_criteria>
- Sidebar shows purple active states, purple brand logo, purple CTA
- TopNav shows purple search focus, purple notification dot, atlas-dark backgrounds
- No orange hex values in either file
- No Manrope font reference in Sidebar
</success_criteria>

<output>
After completion, create `.planning/phases/10-atlas-redesign/10-02-SUMMARY.md`
</output>
