---
phase: 11-cinematic-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/globals.css
  - src/app/layout.tsx
  - src/app/(auth)/layout.tsx
autonomous: true
requirements: [CIN-FOUNDATION]

must_haves:
  truths:
    - "Body background is #050507 with radial ambient gradient blobs"
    - "glass-card class uses rgba(255,255,255,0.05) + backdrop-blur(50px) + lavender border"
    - "cinematic-glow uses inset purple glow + deep drop shadow"
    - "Manrope font is loaded for headlines with font-light tracking-[-0.03em]"
    - "Inter font is loaded for body text"
    - "Material Symbols icons use wght 200 (thin)"
    - "Auth layout uses spacious pt-32 pb-24 px-16 content area"
  artifacts:
    - path: "src/app/globals.css"
      provides: "CSS custom properties + utility classes for cinematic theme"
      contains: "glass-card"
    - path: "src/app/layout.tsx"
      provides: "Manrope + Inter font loading, body classes"
      contains: "Manrope"
    - path: "src/app/(auth)/layout.tsx"
      provides: "Spacious content area with cinematic body"
      contains: "pt-32"
  key_links:
    - from: "src/app/globals.css"
      to: "all components"
      via: "CSS custom properties and utility classes"
      pattern: "glass-card|cinematic-glow"
    - from: "src/app/layout.tsx"
      to: "src/app/globals.css"
      via: "font CSS variables"
      pattern: "font-manrope|font-inter"
---

<objective>
Replace the Atlas Cloud theme foundation with the cinematic luxury design system. Update all CSS custom properties, utility classes, font loading, and layout structure to match the user's HTML reference.

Purpose: Every downstream file depends on these globals -- colors, glass effects, fonts, and spacing must be correct here first.
Output: globals.css with cinematic palette, layout.tsx with Manrope+Inter, auth layout with spacious content area.
</objective>

<execution_context>
@/Users/aasish/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aasish/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-cinematic-redesign/11-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite globals.css with cinematic design tokens and utility classes</name>
  <files>src/app/globals.css</files>
  <read_first>src/app/globals.css</read_first>
  <action>
Completely rewrite globals.css with the cinematic luxury design system. Preserve the @import "tailwindcss" and @theme structure.

**@theme color replacements (exact values):**
- --color-primary: #c6bfff (was #7F72F7)
- --color-primary-container: #8c80ff (was #6B5CF7)
- --color-primary-dim: #8c80ff (was #6B5CF7)
- --color-primary-fixed: #8c80ff (was #6B5CF7)
- --color-primary-fixed-dim: #6b5cf7 (keep, it's deeper)
- --color-on-primary: #1a1a2e (was #FFFFFF -- dark text on light lavender)
- --color-on-primary-container: #e4e1e8 (was #1A1A2E)
- --color-on-primary-fixed: #1a1a2e (was #FFFFFF)
- --color-secondary: #fface8 (was #A78BFA -- pink per CONTEXT)
- --color-secondary-container: #3d2a3a (was #2D2654)
- --color-on-secondary: #1a1a2e (keep)
- --color-on-secondary-container: #ffd6f0 (was #DDD6FE)
- --color-surface: #050507 (was #0A0A0F -- true black)
- --color-surface-bright: #35343a (was #2A2A3E)
- --color-surface-container: #1f1f24 (was #1A1A2E)
- --color-surface-container-high: #2a292f (was #1A1A2E)
- --color-surface-container-highest: #35343a (was #1A1A2E)
- --color-surface-container-low: #1b1b20 (was #12121A)
- --color-surface-container-lowest: #0a0a0e (was #0A0A0F)
- --color-on-surface: #e4e1e8 (was #FFFFFF)
- --color-on-surface-variant: #c8c4d6 (was #A0A0B8)
- --color-error: #ffb4ab (was #EF4444 -- softer error per CONTEXT)
- --color-error-container: #93000a (keep)
- --color-error-dim: #ff897d (was #DC2626)
- --color-on-error: #690005 (was #FFFFFF)
- --color-on-error-container: #ffdad6 (keep)
- --color-outline: #928f9f (ADD -- missing from old theme)
- --color-outline-variant: #474554 (was rgba(127,114,247,0.1))

**Tertiary and status colors:** Keep unchanged (tertiary #81e9ff, success #4caf50, processing #9c27b0).

**Font variables:**
- --font-headline: var(--font-manrope), 'Manrope', sans-serif (was Inter for headlines)
- --font-body: var(--font-inter), 'Inter', sans-serif (keep)
- --font-label: var(--font-inter), 'Inter', sans-serif (keep)

**Utility class rewrites:**

glass-card:
```css
.glass-card {
  background: rgba(255, 255, 255, 0.05);
  backdrop-filter: blur(50px);
  border: 1px solid rgba(198, 191, 255, 0.25);
}
```

glass-panel (same as glass-card):
```css
.glass-panel {
  background: rgba(255, 255, 255, 0.05);
  backdrop-filter: blur(50px);
  border: 1px solid rgba(198, 191, 255, 0.25);
}
```

glass-input:
```css
.glass-input {
  background: rgba(255, 255, 255, 0.05);
  backdrop-filter: blur(50px);
  border: 1px solid rgba(198, 191, 255, 0.15);
}
```

cinematic-glow:
```css
.cinematic-glow {
  box-shadow: inset 0 0 40px rgba(198, 191, 255, 0.08), 0 20px 50px rgba(0, 0, 0, 0.5);
}
```

cinematic-glow-warm (rename to cinematic-glow-accent):
```css
.cinematic-glow-warm {
  box-shadow: inset 0 0 40px rgba(198, 191, 255, 0.12), 0 20px 50px rgba(0, 0, 0, 0.5);
}
```

hero-vignette:
```css
.hero-vignette {
  background: radial-gradient(circle at center, transparent 0%, rgba(5, 5, 7, 0.8) 100%);
}
```

**ADD new radial-ambient class for body background:**
```css
.radial-ambient {
  background:
    radial-gradient(ellipse 600px 400px at 20% 50%, rgba(140, 128, 255, 0.08) 0%, transparent 70%),
    radial-gradient(ellipse 500px 350px at 80% 30%, rgba(255, 172, 232, 0.05) 0%, transparent 70%),
    #050507;
}
```

text-gradient-primary:
```css
.text-gradient-primary {
  background: linear-gradient(135deg, #c6bfff, #8c80ff);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
}
```

headline-font:
```css
.headline-font {
  font-family: 'Manrope', sans-serif;
}
```

Custom scrollbar thumb:
```css
.custom-scrollbar::-webkit-scrollbar-thumb {
  background: rgba(198, 191, 255, 0.15);
  /* ... rest stays same */
}
```

Material Symbols: Change wght to 200:
```css
.material-symbols-outlined {
  font-variation-settings: 'FILL' 0, 'wght' 200, 'GRAD' 0, 'opsz' 24;
  vertical-align: middle;
}
```

Keep all animation keyframes (shimmerAnimation, slideUp) and shimmer classes unchanged.
  </action>
  <acceptance_criteria>
    - globals.css contains "--color-primary: #c6bfff"
    - globals.css contains "--color-surface: #050507"
    - globals.css contains "--color-on-surface: #e4e1e8"
    - globals.css contains "--color-on-surface-variant: #c8c4d6"
    - globals.css contains "--color-outline: #928f9f"
    - globals.css contains "--color-outline-variant: #474554"
    - globals.css contains "--color-error: #ffb4ab"
    - globals.css contains "--color-secondary: #fface8"
    - globals.css contains "backdrop-filter: blur(50px)" in glass-card
    - globals.css contains "rgba(198, 191, 255, 0.25)" in glass-card
    - globals.css contains "rgba(198, 191, 255, 0.08)" in cinematic-glow
    - globals.css contains "radial-ambient"
    - globals.css contains "'wght' 200" in material-symbols
    - globals.css contains "--font-headline: var(--font-manrope)"
    - globals.css does NOT contain "#7F72F7"
    - globals.css does NOT contain "#A78BFA" (except as comment if any)
    - globals.css does NOT contain "#0A0A0F" (except surface-container-lowest which is #0a0a0e)
  </acceptance_criteria>
  <verify>
    <automated>cd "/Users/aasish/Desktop/CLAUDE PROJECTS/NYRADNA/NYRADNA/director" && grep -c "#c6bfff" src/app/globals.css && grep -c "#050507" src/app/globals.css && grep -c "blur(50px)" src/app/globals.css && grep -c "radial-ambient" src/app/globals.css && grep -c "'wght' 200" src/app/globals.css && ! grep -q "#7F72F7" src/app/globals.css && echo "PASS" || echo "FAIL: old colors still present"</automated>
  </verify>
  <done>globals.css uses the complete cinematic luxury palette with all design tokens, glass-card/cinematic-glow/radial-ambient utility classes, Manrope headline font, and thin Material Symbols icons. Zero occurrences of old Atlas hex values.</done>
</task>

<task type="auto">
  <name>Task 2: Update root layout fonts and auth layout spacing</name>
  <files>src/app/layout.tsx, src/app/(auth)/layout.tsx</files>
  <read_first>src/app/layout.tsx, src/app/(auth)/layout.tsx</read_first>
  <action>
**src/app/layout.tsx changes:**

1. Manrope font config -- expand weights to include 300 (light) for headlines:
```tsx
const manrope = Manrope({
  subsets: ['latin'],
  weight: ['300', '400', '700'],
  variable: '--font-manrope',
})
```

2. Inter font config -- expand weights:
```tsx
const inter = Inter({
  subsets: ['latin'],
  weight: ['400', '500', '600', '700'],
  variable: '--font-inter',
})
```

3. Body className -- add radial-ambient class:
```tsx
<body className={`${manrope.variable} ${inter.variable} font-sans bg-surface text-on-surface antialiased radial-ambient`}>
```

Keep everything else (metadata, AuthProvider, html lang, Material Symbols link) unchanged.

**src/app/(auth)/layout.tsx changes:**

1. Update content area spacing from `pt-16` to `pt-32 pb-24 px-16` for cinematic spaciousness:
```tsx
<div className="md:ml-64 pt-32 pb-24 px-16 min-h-screen">
```

2. Keep the rest unchanged (Sidebar, TopNav, AuthGuard, ErrorBoundary, WizardProvider, EnvBanner).
  </action>
  <acceptance_criteria>
    - layout.tsx contains Manrope with weight '300'
    - layout.tsx contains "radial-ambient" in body className
    - layout.tsx body text color is "text-on-surface" (not "text-white")
    - auth/layout.tsx contains "pt-32 pb-24 px-16"
    - auth/layout.tsx does NOT contain "pt-16 min-h-screen" (old spacing)
  </acceptance_criteria>
  <verify>
    <automated>cd "/Users/aasish/Desktop/CLAUDE PROJECTS/NYRADNA/NYRADNA/director" && grep -q "radial-ambient" src/app/layout.tsx && grep -q "'300'" src/app/layout.tsx && grep -q "pt-32 pb-24 px-16" "src/app/(auth)/layout.tsx" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Root layout loads Manrope (300/400/700) + Inter (400-700) fonts and applies radial-ambient body class. Auth layout uses spacious pt-32 pb-24 px-16 content area.</done>
</task>

</tasks>

<verification>
- `npm run build` completes without errors (CSS + layout compile correctly)
- Body renders with #050507 background and faint radial purple/pink blobs
- Glass-card class produces translucent panels with lavender border
- Manrope font loads from Google Fonts
- Material Symbols render with thin weight (200)
</verification>

<success_criteria>
All CSS custom properties use the cinematic palette. glass-card, cinematic-glow, and radial-ambient classes match the user's HTML reference. Manrope is the headline font. Auth layout has generous spacing. Zero occurrences of #7F72F7 or #A78BFA in globals.css.
</success_criteria>

<output>
After completion, create `.planning/phases/11-cinematic-redesign/11-01-SUMMARY.md`
</output>
