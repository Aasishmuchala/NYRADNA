---
phase: 11-cinematic-redesign
plan: 06
type: execute
wave: 4
depends_on: [11-01, 11-02]
files_modified:
  - src/components/assets/AssetSetCard.tsx
  - src/components/assets/AssetItemGrid.tsx
  - src/components/assets/FileUploadZone.tsx
  - src/components/assets/GeneratedAssetsPanel.tsx
  - src/app/(auth)/dashboard/page.tsx
  - src/app/(auth)/projects/page.tsx
  - src/app/(auth)/settings/page.tsx
  - src/app/(auth)/characters/page.tsx
  - src/app/(auth)/assets/page.tsx
  - src/app/page.tsx
  - src/components/ui/Modal.tsx
autonomous: true
requirements: [CIN-ASSETS-AND-REMAINING]

must_haves:
  truths:
    - "All 4 asset components have zero hardcoded Atlas hex values"
    - "All non-create pages have zero hardcoded Atlas hex values"
    - "Landing page headings use font-light tracking-[-0.03em]"
    - "Landing page buttons are rounded-full with glow"
    - "Dashboard, projects, characters, settings pages use glass cards"
    - "Modal uses glass-card styling"
    - "All headings across remaining pages use font-light tracking-[-0.03em]"
  artifacts:
    - path: "src/components/assets/AssetSetCard.tsx"
      provides: "Cinematic asset set card"
    - path: "src/components/assets/GeneratedAssetsPanel.tsx"
      provides: "Cinematic generated assets panel"
    - path: "src/app/page.tsx"
      provides: "Cinematic landing page"
    - path: "src/app/(auth)/dashboard/page.tsx"
      provides: "Cinematic dashboard"
  key_links:
    - from: "all asset components"
      to: "src/app/globals.css"
      via: "CSS custom properties"
      pattern: "glass-card|text-primary"
    - from: "src/app/page.tsx"
      to: "src/app/globals.css"
      via: "CSS custom properties and utility classes"
      pattern: "glass-card|radial-ambient|cinematic-glow"
---

<objective>
Restyle all asset components, remaining non-create pages (dashboard, projects, settings, characters, assets, landing), and the Modal UI component with the cinematic luxury aesthetic. This completes the full-app redesign.

Purpose: Every single file with UI code must use the cinematic palette -- no Atlas remnants anywhere.
Output: 11 files fully converted, completing the entire app redesign.
</objective>

<execution_context>
@/Users/aasish/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aasish/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-cinematic-redesign/11-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle 4 asset components and Modal</name>
  <files>src/components/assets/AssetSetCard.tsx, src/components/assets/AssetItemGrid.tsx, src/components/assets/FileUploadZone.tsx, src/components/assets/GeneratedAssetsPanel.tsx, src/components/ui/Modal.tsx</files>
  <read_first>src/components/assets/AssetSetCard.tsx, src/components/assets/AssetItemGrid.tsx, src/components/assets/FileUploadZone.tsx, src/components/assets/GeneratedAssetsPanel.tsx, src/components/ui/Modal.tsx</read_first>
  <action>
Read each file first, then apply all cinematic replacements.

**=== UNIVERSAL COLOR REPLACEMENTS ===**

| Find | Replace With |
|------|-------------|
| `bg-[#1A1A2E]` | `bg-white/5` or glass-card (for larger panels) |
| `bg-[#12121A]` | `bg-white/[0.03]` |
| `bg-[#0A0A0F]` | `bg-white/[0.03]` |
| `border-[#7F72F7]/10` | `border-white/10` |
| `border-[#7F72F7]` (solid) | `border-primary` |
| `text-[#7F72F7]` | `text-primary` |
| `text-[#A0A0B8]` | `text-on-surface-variant` |
| `text-[#A78BFA]` | `text-primary` |
| `text-[#555]` | `text-outline` |
| `bg-[#7F72F7]` | `bg-primary` |
| `bg-[#7F72F7]/10` | `bg-primary/10` |
| `hover:bg-[#7F72F7]/10` | `hover:bg-primary/10` |
| `focus:border-[#7F72F7]` | `focus:border-primary/30` |
| `placeholder-[#555]` | `placeholder-outline` |

**=== TYPOGRAPHY REPLACEMENTS ===**

| Find | Replace With |
|------|-------------|
| `font-headline font-black` | `font-headline font-light tracking-[-0.03em]` |
| `font-headline font-bold` (on headings) | `font-headline font-light tracking-[-0.03em]` |
| `font-bold` on h3/h4 heading text | `font-light tracking-[-0.03em]` |

**=== SPECIFIC TO AssetSetCard.tsx (59 lines) ===**
1. Card container: use bg-white/5 with border border-white/10 rounded-xl
2. Any hardcoded colors: apply table above
3. Hover state: hover:bg-white/10 or hover:border-primary/30

**=== SPECIFIC TO AssetItemGrid.tsx (198 lines) ===**
1. Grid item containers: bg-white/5 border border-white/10
2. Drag overlay: bg-white/10 backdrop-blur-sm
3. Any hardcoded #7F72F7 or #A0A0B8: apply table above
4. Empty state: bg-white/[0.02] border-dashed border-white/10

**=== SPECIFIC TO FileUploadZone.tsx (103 lines) ===**
1. Drop zone: bg-white/[0.02] border-2 border-dashed border-white/10
2. Active drop zone: border-primary bg-primary/5
3. Any hardcoded colors: apply table above
4. Upload icon: text-on-surface-variant

**=== SPECIFIC TO GeneratedAssetsPanel.tsx (185 lines) ===**
1. Panel container: glass-card (this is a larger panel)
2. Header: font-light tracking-[-0.03em]
3. Asset items: bg-white/5 border border-white/10
4. Provenance badges: keep amber/purple distinction but update hex values if hardcoded
5. All text-[#A0A0B8] -> text-on-surface-variant
6. All text-[#555] -> text-outline
7. All bg-[#12121A] -> bg-white/[0.03]

**=== SPECIFIC TO Modal.tsx ===**
1. Dialog container: already uses Tailwind semantic colors (`bg-surface-container-high text-on-surface border-outline-variant/20`).
   Update to: `glass-card text-on-surface rounded-2xl p-0 backdrop:bg-black/60 backdrop:backdrop-blur-sm`
   Note: glass-card provides the bg, border, and blur. Remove the separate `border border-outline-variant/20` since glass-card includes border.
2. Heading: `font-bold font-headline` -> `font-light tracking-[-0.03em] font-headline`
3. Cancel button: `bg-surface-container-highest` -> `bg-white/10`
4. OK/Confirm button: keep `bg-primary text-on-primary` (CSS vars handle colors)
5. Danger button: keep `bg-error text-on-error` (CSS vars handle colors)
  </action>
  <acceptance_criteria>
    - AssetSetCard.tsx does NOT contain "#7F72F7"
    - AssetSetCard.tsx does NOT contain "#1A1A2E"
    - AssetItemGrid.tsx does NOT contain "#7F72F7"
    - AssetItemGrid.tsx does NOT contain "#12121A"
    - FileUploadZone.tsx does NOT contain "#7F72F7"
    - GeneratedAssetsPanel.tsx does NOT contain "#7F72F7"
    - GeneratedAssetsPanel.tsx does NOT contain "#A78BFA"
    - GeneratedAssetsPanel.tsx does NOT contain "#12121A"
    - Modal.tsx contains "glass-card"
    - Modal.tsx contains "tracking-[-0.03em]"
  </acceptance_criteria>
  <verify>
    <automated>cd "/Users/aasish/Desktop/CLAUDE PROJECTS/NYRADNA/NYRADNA/director" && PASS=true; for f in src/components/assets/AssetSetCard.tsx src/components/assets/AssetItemGrid.tsx src/components/assets/FileUploadZone.tsx src/components/assets/GeneratedAssetsPanel.tsx src/components/ui/Modal.tsx; do if grep -qE "#7F72F7|#A78BFA|#1A1A2E|#12121A|#0A0A0F" "$f" 2>/dev/null; then echo "FAIL: $f"; PASS=false; fi; done; $PASS && echo "ALL PASS" || echo "SOME FAILED"</automated>
  </verify>
  <done>All 4 asset components and Modal use cinematic palette. Glass backgrounds, lavender borders, and cinematic typography throughout. Zero hardcoded Atlas hex values.</done>
</task>

<task type="auto">
  <name>Task 2: Restyle dashboard, projects, settings, characters, assets pages, and landing page</name>
  <files>src/app/(auth)/dashboard/page.tsx, src/app/(auth)/projects/page.tsx, src/app/(auth)/settings/page.tsx, src/app/(auth)/characters/page.tsx, src/app/(auth)/assets/page.tsx, src/app/page.tsx</files>
  <read_first>src/app/(auth)/dashboard/page.tsx, src/app/(auth)/projects/page.tsx, src/app/(auth)/settings/page.tsx, src/app/(auth)/characters/page.tsx, src/app/(auth)/assets/page.tsx, src/app/page.tsx</read_first>
  <action>
Read each file first, then apply all cinematic replacements.

**=== UNIVERSAL COLOR REPLACEMENTS (same table) ===**

| Find | Replace With |
|------|-------------|
| `bg-[#1A1A2E]` | `glass-card` class or `bg-white/5` |
| `bg-[#12121A]` | `bg-white/[0.03]` |
| `bg-[#0A0A0F]` | `bg-white/[0.03]` |
| `border-[#7F72F7]/10` | `border-white/10` |
| `border-[#7F72F7]` (solid) | `border-primary` |
| `text-[#7F72F7]` | `text-primary` |
| `text-[#A0A0B8]` | `text-on-surface-variant` |
| `text-[#555]` | `text-outline` |
| `bg-[#7F72F7]` | `bg-primary` |
| `focus:border-[#7F72F7]` | `focus:border-primary/30` |
| `hover:bg-[#7F72F7]/10` | `hover:bg-primary/10` |
| `placeholder-[#555]` | `placeholder-outline` |

**=== TYPOGRAPHY REPLACEMENTS ===**

| Find | Replace With |
|------|-------------|
| `font-headline font-black` | `font-headline font-light tracking-[-0.03em]` |
| `font-headline font-bold` (on headings) | `font-headline font-light tracking-[-0.03em]` |
| `font-black tracking-tighter` | `font-light tracking-[-0.03em]` |
| `font-black tracking-tight` | `font-light tracking-[-0.03em]` |
| `font-bold tracking-tight` | `font-light tracking-[-0.03em]` |

**=== SPECIFIC TO landing page (src/app/page.tsx) ===**

This page uses mostly Tailwind semantic colors already. Update:

1. Brand text "DIRECTOR": `font-black tracking-tighter` -> `font-light tracking-[-0.03em]`
2. Hero h1: `font-black font-headline tracking-tighter` -> `font-light font-headline tracking-[-0.03em]`
   Also increase size: `text-5xl md:text-7xl lg:text-8xl` -> keep or use `text-6xl md:text-8xl` for more cinematic scale
3. CTA "Start for free" button: add `rounded-full` (was `rounded-lg`), add `shadow-[0_0_30px_rgba(198,191,255,0.2)]`
4. "Sign in" button: `rounded-lg` -> `rounded-full`
5. Trust section heading: `font-headline text-3xl font-bold tracking-tight` -> `font-headline text-4xl font-light tracking-[-0.03em]`
6. Case study label: already `text-xs font-bold uppercase tracking-widest text-primary` -- this is fine
7. Case study h3: `font-bold font-headline` -> `font-light tracking-[-0.03em] font-headline`
8. CTA module h3: `font-headline text-4xl font-black tracking-tight` -> `font-headline text-5xl font-light tracking-[-0.03em]`
9. CTA "Claim Your First Render" button: `rounded-lg` -> `rounded-full`, add glow shadow
10. Footer: keep as-is (already uses semantic colors)

**=== SPECIFIC TO dashboard/page.tsx (339 lines) ===**

1. All card containers: glass-card class
2. Headings: font-light tracking-[-0.03em]
3. Labels: text-[10px] tracking-[0.2em] uppercase font-bold
4. Apply all hex color replacements from table
5. Action buttons: rounded-full with primary styling

**=== SPECIFIC TO projects/page.tsx (317 lines) ===**

1. Project cards: glass-card with hover:bg-white/10
2. Headings: font-light tracking-[-0.03em]
3. Apply all hex color replacements

**=== SPECIFIC TO settings/page.tsx (900 lines) ===**

This is a large file. Apply all universal replacements thoroughly:
1. Section cards: glass-card
2. Form inputs: bg-white/[0.03] border border-white/10
3. Toggle/select elements: update borders and backgrounds
4. All headings: font-light tracking-[-0.03em]
5. All labels: ensure text-[10px] tracking-[0.2em] pattern
6. Save buttons: bg-primary text-on-primary rounded-full

**=== SPECIFIC TO characters/page.tsx (220 lines) ===**

1. Character cards: glass-card or bg-white/5 border border-white/10
2. Headings: font-light tracking-[-0.03em]
3. Apply all hex color replacements

**=== SPECIFIC TO assets/page.tsx (528 lines) ===**

1. Asset grid containers: glass-card or bg-white/5
2. Headings: font-light tracking-[-0.03em]
3. Upload/action buttons: rounded-full or rounded-xl
4. Apply all hex color replacements
  </action>
  <acceptance_criteria>
    - page.tsx (landing) contains "tracking-[-0.03em]"
    - page.tsx (landing) contains "rounded-full" on CTA buttons
    - dashboard/page.tsx does NOT contain "#7F72F7" or "#1A1A2E"
    - projects/page.tsx does NOT contain "#7F72F7" or "#1A1A2E"
    - settings/page.tsx does NOT contain "#7F72F7" or "#1A1A2E"
    - characters/page.tsx does NOT contain "#7F72F7" or "#1A1A2E"
    - assets/page.tsx does NOT contain "#7F72F7" or "#1A1A2E"
    - page.tsx (landing) heading does NOT contain "font-black tracking-tighter"
  </acceptance_criteria>
  <verify>
    <automated>cd "/Users/aasish/Desktop/CLAUDE PROJECTS/NYRADNA/NYRADNA/director" && PASS=true; for f in src/app/page.tsx "src/app/(auth)/dashboard/page.tsx" "src/app/(auth)/projects/page.tsx" "src/app/(auth)/settings/page.tsx" "src/app/(auth)/characters/page.tsx" "src/app/(auth)/assets/page.tsx"; do if grep -qE "#7F72F7|#A78BFA|#1A1A2E|#12121A|#0A0A0F" "$f" 2>/dev/null; then echo "FAIL: $f"; PASS=false; fi; done; $PASS && echo "ALL PASS" || echo "SOME FAILED"</automated>
  </verify>
  <done>All 6 pages (landing, dashboard, projects, settings, characters, assets) fully converted to cinematic aesthetic. Landing page has luxurious font-light headings and rounded-full CTA buttons with glow.</done>
</task>

</tasks>

<verification>
- Zero occurrences of #7F72F7, #A78BFA, #1A1A2E, #12121A, #0A0A0F across ALL 11 files
- Landing page feels cinematic with light headings and rounded-full glowing CTAs
- Dashboard, projects, characters pages use glass cards
- Settings page uses glass inputs and cards
- `npm run build` completes without errors
</verification>

<success_criteria>
All 11 remaining files (4 asset components, Modal, 6 pages) are fully converted to the cinematic luxury aesthetic. This completes the entire app redesign -- zero Atlas hex values remain anywhere in the codebase.
</success_criteria>

<output>
After completion, create `.planning/phases/11-cinematic-redesign/11-06-SUMMARY.md`
</output>
