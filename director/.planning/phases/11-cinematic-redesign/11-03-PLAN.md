---
phase: 11-cinematic-redesign
plan: 03
type: execute
wave: 3
depends_on: [11-01, 11-02]
files_modified:
  - src/app/(auth)/create/directors-cut/page.tsx
  - src/app/(auth)/create/character-setup/page.tsx
  - src/app/(auth)/create/generating/page.tsx
  - src/app/(auth)/create/review/page.tsx
  - src/app/(auth)/create/export/page.tsx
autonomous: true
requirements: [CIN-CREATE-PAGES-A]

must_haves:
  truths:
    - "All hardcoded #7F72F7 replaced with #c6bfff or primary CSS var"
    - "All hardcoded #A0A0B8 replaced with #c8c4d6 or on-surface-variant CSS var"
    - "All bg-[#1A1A2E] cards replaced with glass-card class"
    - "All bg-[#12121A] elements replaced with bg-white/5 or glass equivalent"
    - "All bg-[#0A0A0F] inputs replaced with bg-white/[0.03] with border-white/10"
    - "All headings use font-headline font-light tracking-[-0.03em]"
    - "All labels use text-[10px] tracking-[0.2em] uppercase font-bold"
    - "Progress bars use #c6bfff instead of #7F72F7"
    - "Cards have p-10 spacing for cinematic spaciousness"
  artifacts:
    - path: "src/app/(auth)/create/directors-cut/page.tsx"
      provides: "Cinematic directors cut page"
    - path: "src/app/(auth)/create/character-setup/page.tsx"
      provides: "Cinematic character setup page"
    - path: "src/app/(auth)/create/generating/page.tsx"
      provides: "Cinematic generating page"
    - path: "src/app/(auth)/create/review/page.tsx"
      provides: "Cinematic review page"
    - path: "src/app/(auth)/create/export/page.tsx"
      provides: "Cinematic export page"
  key_links:
    - from: "all create pages"
      to: "src/app/globals.css"
      via: "glass-card class and CSS custom properties"
      pattern: "glass-card|text-primary|bg-primary"
---

<objective>
Restyle the 5 heaviest create/* pages (directors-cut, character-setup, generating, review, export) with the cinematic luxury aesthetic. These pages have the most hardcoded hex values and need comprehensive color/typography/spacing updates.

Purpose: These are the most-used pages in the creation workflow. They must feel like a high-end creative suite.
Output: 5 create pages fully converted to cinematic palette with glass cards, spacious layouts, and cinematic typography.
</objective>

<execution_context>
@/Users/aasish/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aasish/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-cinematic-redesign/11-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle directors-cut and character-setup pages</name>
  <files>src/app/(auth)/create/directors-cut/page.tsx, src/app/(auth)/create/character-setup/page.tsx</files>
  <read_first>src/app/(auth)/create/directors-cut/page.tsx, src/app/(auth)/create/character-setup/page.tsx</read_first>
  <action>
Apply the following mechanical replacements to BOTH files. Read each file first, then apply all changes.

**=== UNIVERSAL COLOR REPLACEMENTS (apply to every occurrence) ===**

| Find | Replace With |
|------|-------------|
| `bg-[#1A1A2E]` | `glass-card` (add as className, remove inline bg) |
| `bg-[#12121A]` | `bg-white/5` |
| `bg-[#12121A]/40` | `bg-white/[0.03]` |
| `bg-[#0A0A0F]` | `bg-white/[0.03]` |
| `border-[#7F72F7]/10` | `border-white/10` |
| `border-[#7F72F7]` (solid border) | `border-primary` |
| `text-[#7F72F7]` | `text-primary` |
| `text-[#A0A0B8]` | `text-on-surface-variant` |
| `text-[#555]` | `text-outline` |
| `text-[#A0A0B8]/30` | `text-on-surface-variant/30` |
| `bg-[#7F72F7]` | `bg-primary` |
| `bg-[#7F72F7]/10` | `bg-primary/10` |
| `bg-[#7F72F7]/5` | `bg-primary/5` |
| `focus:border-[#7F72F7]` | `focus:border-primary/30` |
| `hover:bg-[#7F72F7]/10` | `hover:bg-primary/10` |
| `placeholder-[#555]` | `placeholder-outline` |

**=== TYPOGRAPHY REPLACEMENTS ===**

| Find | Replace With |
|------|-------------|
| `font-headline font-black tracking-tighter` | `font-headline font-light tracking-[-0.03em]` |
| `font-headline font-bold` (on h2 headings) | `font-headline font-light tracking-[-0.03em]` |
| `font-headline font-extrabold` | `font-headline font-light tracking-[-0.03em]` |
| `font-bold text-white` (on h1/h2) | `font-light tracking-[-0.03em] text-on-surface` |

**=== SPECIFIC TO directors-cut/page.tsx ===**

1. Progress bar track: `bg-[#12121A]` -> `bg-white/5`
2. Progress bar fill: `bg-[#7F72F7]` -> `bg-primary`
3. Progress bar glow: `shadow-[0_0_40px_rgba(127,114,247,0.1)]` -> `shadow-[0_0_40px_rgba(198,191,255,0.1)]`
4. Card containers (bg-[#1A1A2E] with border): Replace the ENTIRE className with `glass-card rounded-xl p-8 space-y-4` (increase padding from p-6 to p-8 for spaciousness)
5. Input fields: Replace `bg-[#0A0A0F] border border-[#7F72F7]/10` with `bg-white/[0.03] border border-white/10`
6. Chip tags (bg-[#12121A] rounded-full): Replace with `bg-white/5 rounded-full`
7. Add buttons (border border-[#7F72F7] text-[#7F72F7]): Replace with `border border-primary text-primary`
8. Beat number circles (bg-[#12121A] text-[#7F72F7]): Replace with `bg-white/5 text-primary`
9. Intelligence Feed sidebar: Replace `bg-[#1A1A2E]/50 backdrop-blur-md border border-[#7F72F7]/10` with `glass-card`
10. Intelligence Feed section borders (border-l-2 border-[#7F72F7]): Replace with `border-l-2 border-primary`
11. Empty state containers: Replace `bg-[#0A0A0F] border border-dashed border-[#7F72F7]/10` with `bg-white/[0.02] border border-dashed border-white/10`
12. Sequence Plan segment mono text: `text-[#7F72F7]` -> `text-primary`
13. Footer border: `border-[#7F72F7]/10` -> `border-white/10`
14. Hint text: `text-[#81e9ff]` -> keep (tertiary stays)
15. Locked state: `text-[#555]` -> `text-outline`, `text-[#A0A0B8]` -> `text-on-surface-variant`

**=== SPECIFIC TO character-setup/page.tsx ===**

Apply all universal replacements above. This is the largest file (1541 lines) so be thorough. Additionally:
1. All card containers: replace with glass-card class + increased padding
2. All form inputs: bg-white/[0.03] border border-white/10
3. All section headings: font-headline font-light tracking-[-0.03em]
4. All label text: ensure text-[10px] tracking-[0.2em] uppercase font-bold pattern
5. All chip/tag elements: bg-white/5
6. All icon colors that use #7F72F7: use text-primary
7. Keep any status/semantic colors (green check, red error) unchanged
  </action>
  <acceptance_criteria>
    - directors-cut/page.tsx does NOT contain "#7F72F7"
    - directors-cut/page.tsx does NOT contain "#A0A0B8"
    - directors-cut/page.tsx does NOT contain "#12121A"
    - directors-cut/page.tsx does NOT contain "#0A0A0F"
    - directors-cut/page.tsx does NOT contain "#1A1A2E"
    - directors-cut/page.tsx does NOT contain "#555" (as hex color)
    - directors-cut/page.tsx contains "glass-card"
    - directors-cut/page.tsx contains "tracking-[-0.03em]"
    - character-setup/page.tsx does NOT contain "#7F72F7"
    - character-setup/page.tsx does NOT contain "#1A1A2E"
    - character-setup/page.tsx does NOT contain "#12121A"
    - character-setup/page.tsx contains "glass-card"
    - character-setup/page.tsx contains "tracking-[-0.03em]"
  </acceptance_criteria>
  <verify>
    <automated>cd "/Users/aasish/Desktop/CLAUDE PROJECTS/NYRADNA/NYRADNA/director" && for f in "src/app/(auth)/create/directors-cut/page.tsx" "src/app/(auth)/create/character-setup/page.tsx"; do echo "=== $f ==="; grep -c "glass-card" "$f"; if grep -qE "#7F72F7|#1A1A2E|#12121A|#0A0A0F" "$f"; then echo "FAIL: old colors found"; grep -n "#7F72F7\|#1A1A2E\|#12121A\|#0A0A0F" "$f" | head -5; else echo "PASS: no old colors"; fi; done</automated>
  </verify>
  <done>directors-cut and character-setup pages use glass-card containers, cinematic typography, and zero hardcoded Atlas hex values. All inputs use glass styling, all labels use wide tracking.</done>
</task>

<task type="auto">
  <name>Task 2: Restyle generating, review, and export pages</name>
  <files>src/app/(auth)/create/generating/page.tsx, src/app/(auth)/create/review/page.tsx, src/app/(auth)/create/export/page.tsx</files>
  <read_first>src/app/(auth)/create/generating/page.tsx, src/app/(auth)/create/review/page.tsx, src/app/(auth)/create/export/page.tsx</read_first>
  <action>
Apply the SAME universal color and typography replacements from Task 1 to all three files. Read each file first, then apply all changes.

**=== UNIVERSAL COLOR REPLACEMENTS (same table as Task 1) ===**

| Find | Replace With |
|------|-------------|
| `bg-[#1A1A2E]` | `glass-card` class |
| `bg-[#12121A]` | `bg-white/5` |
| `bg-[#12121A]/40` | `bg-white/[0.03]` |
| `bg-[#0A0A0F]` | `bg-white/[0.03]` |
| `border-[#7F72F7]/10` | `border-white/10` |
| `border-[#7F72F7]` (solid) | `border-primary` |
| `text-[#7F72F7]` | `text-primary` |
| `text-[#A0A0B8]` | `text-on-surface-variant` |
| `text-[#555]` | `text-outline` |
| `bg-[#7F72F7]` | `bg-primary` |
| `focus:border-[#7F72F7]` | `focus:border-primary/30` |
| `hover:bg-[#7F72F7]/10` | `hover:bg-primary/10` |
| `placeholder-[#555]` | `placeholder-outline` |

**=== TYPOGRAPHY REPLACEMENTS (same as Task 1) ===**

| Find | Replace With |
|------|-------------|
| `font-headline font-black` | `font-headline font-light tracking-[-0.03em]` |
| `font-headline font-bold` (on headings) | `font-headline font-light tracking-[-0.03em]` |
| `font-headline font-extrabold` | `font-headline font-light tracking-[-0.03em]` |

**=== SPECIFIC TO generating/page.tsx ===**
1. Scene pipeline cards: replace solid bg with glass-card
2. Progress indicators: use primary color (#c6bfff via CSS var)
3. Act structure sections: use glass-card for containers
4. Keep any `animate-pulse` and `animate-spin` intact
5. Keep the purple Generate Videos button colors if present (processing theme)

**=== SPECIFIC TO review/page.tsx ===**
1. Scene grid cards: replace with glass-card
2. CLIP score badges: keep semantic colors but update borders
3. Grid view containers: glass-card with p-8 padding

**=== SPECIFIC TO export/page.tsx ===**
1. Export option cards: replace with glass-card
2. Download buttons: ensure primary color scheme
3. Preview containers: glass-card styling

For ALL three files:
- Replace ALL progress bar tracks: `bg-[#12121A]` -> `bg-white/5`
- Replace ALL progress bar fills: `bg-[#7F72F7]` -> `bg-primary`
- Replace ALL card padding: increase p-6 to p-8 where it's a major section card
- Replace ALL heading styles to font-light tracking-[-0.03em]
- Ensure labels have text-[10px] tracking-[0.2em] uppercase font-bold pattern
  </action>
  <acceptance_criteria>
    - generating/page.tsx does NOT contain "#7F72F7"
    - generating/page.tsx does NOT contain "#1A1A2E"
    - generating/page.tsx does NOT contain "#12121A"
    - generating/page.tsx contains "glass-card" OR "bg-white/"
    - review/page.tsx does NOT contain "#7F72F7"
    - review/page.tsx does NOT contain "#1A1A2E"
    - review/page.tsx contains "glass-card" OR "bg-white/"
    - export/page.tsx does NOT contain "#7F72F7"
    - export/page.tsx does NOT contain "#1A1A2E"
    - export/page.tsx contains "glass-card" OR "bg-white/"
  </acceptance_criteria>
  <verify>
    <automated>cd "/Users/aasish/Desktop/CLAUDE PROJECTS/NYRADNA/NYRADNA/director" && PASS=true; for f in "src/app/(auth)/create/generating/page.tsx" "src/app/(auth)/create/review/page.tsx" "src/app/(auth)/create/export/page.tsx"; do echo "=== $f ==="; if grep -qE "#7F72F7|#1A1A2E|#12121A|#0A0A0F" "$f"; then echo "FAIL: old colors found"; PASS=false; else echo "PASS: no old colors"; fi; done; $PASS && echo "ALL PASS" || echo "SOME FAILED"</automated>
  </verify>
  <done>generating, review, and export pages use glass-card containers, cinematic typography, and zero hardcoded Atlas hex values.</done>
</task>

</tasks>

<verification>
- Zero occurrences of #7F72F7, #1A1A2E, #12121A, #0A0A0F, #A0A0B8 in any of the 5 files
- All 5 pages use glass-card class for containers
- All headings use font-light tracking-[-0.03em]
- `npm run build` completes without errors
</verification>

<success_criteria>
All 5 heavy create pages (directors-cut, character-setup, generating, review, export) are fully converted to the cinematic luxury aesthetic. Zero hardcoded Atlas hex values remain. Glass cards, cinematic typography, and spacious layouts throughout.
</success_criteria>

<output>
After completion, create `.planning/phases/11-cinematic-redesign/11-03-SUMMARY.md`
</output>
