---
phase: 11-cinematic-redesign
plan: 04
type: execute
wave: 3
depends_on: [11-01, 11-02]
files_modified:
  - src/app/(auth)/create/intent/page.tsx
  - src/app/(auth)/create/brief/page.tsx
  - src/app/(auth)/create/style-dna/page.tsx
  - src/app/(auth)/create/asset-sets/page.tsx
  - src/app/(auth)/create/pipeline/page.tsx
autonomous: true
requirements: [CIN-CREATE-PAGES-B]

must_haves:
  truths:
    - "All hardcoded #7F72F7 replaced with #c6bfff or primary CSS var"
    - "All hardcoded #A0A0B8 replaced with #c8c4d6 or on-surface-variant CSS var"
    - "All bg-[#1A1A2E] replaced with glass-card class"
    - "All bg-[#12121A] replaced with bg-white/5"
    - "All headings use font-headline font-light tracking-[-0.03em]"
    - "Chat bubbles use glass styling instead of solid backgrounds"
    - "Starter chips use glass styling"
  artifacts:
    - path: "src/app/(auth)/create/intent/page.tsx"
      provides: "Cinematic intent/chat page"
    - path: "src/app/(auth)/create/brief/page.tsx"
      provides: "Cinematic brief page"
    - path: "src/app/(auth)/create/style-dna/page.tsx"
      provides: "Cinematic style DNA page"
    - path: "src/app/(auth)/create/asset-sets/page.tsx"
      provides: "Cinematic asset sets page"
    - path: "src/app/(auth)/create/pipeline/page.tsx"
      provides: "Cinematic pipeline page"
  key_links:
    - from: "all create pages"
      to: "src/app/globals.css"
      via: "glass-card class and CSS custom properties"
      pattern: "glass-card|text-primary|bg-primary"
---

<objective>
Restyle the 5 lighter create/* pages (intent, brief, style-dna, asset-sets, pipeline) with the cinematic luxury aesthetic. These pages are smaller but still have hardcoded Atlas hex values that need replacement.

Purpose: Complete the create/* workflow conversion -- every step must feel cinematic.
Output: 5 create pages fully converted to cinematic palette.
</objective>

<execution_context>
@/Users/aasish/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aasish/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-cinematic-redesign/11-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle intent (chat) and brief pages</name>
  <files>src/app/(auth)/create/intent/page.tsx, src/app/(auth)/create/brief/page.tsx</files>
  <read_first>src/app/(auth)/create/intent/page.tsx, src/app/(auth)/create/brief/page.tsx</read_first>
  <action>
Apply the following changes to BOTH files. Read each file first, then apply all changes.

**=== UNIVERSAL COLOR REPLACEMENTS ===**

| Find | Replace With |
|------|-------------|
| `bg-[#1A1A2E]` | `glass-card` class |
| `bg-[#12121A]` | `bg-white/5` |
| `bg-[#0A0A0F]` | `bg-white/[0.03]` |
| `border-[#7F72F7]/10` | `border-white/10` |
| `border-[#7F72F7]` (solid) | `border-primary` |
| `text-[#7F72F7]` | `text-primary` |
| `text-[#A0A0B8]` | `text-on-surface-variant` |
| `text-[#555]` | `text-outline` |
| `bg-[#7F72F7]` | `bg-primary` |
| `focus:border-[#7F72F7]` | `focus:border-primary/30` |
| `hover:bg-[#7F72F7]/10` | `hover:bg-primary/10` |
| `placeholder-[#555]` | `placeholder-outline` |

**=== TYPOGRAPHY REPLACEMENTS ===**

| Find | Replace With |
|------|-------------|
| `font-headline font-black` | `font-headline font-light tracking-[-0.03em]` |
| `font-headline font-bold` (on headings) | `font-headline font-light tracking-[-0.03em]` |
| `font-headline font-extrabold` | `font-headline font-light tracking-[-0.03em]` |
| `font-bold text-xs uppercase tracking-[0.2em]` label pattern | `text-[10px] font-bold uppercase tracking-[0.2em]` |

**=== SPECIFIC TO intent/page.tsx ===**

1. Background ambient blob: `bg-primary/5` is fine (uses CSS var)
2. Header label: `text-primary font-headline font-bold text-xs uppercase tracking-[0.2em]` -> `text-primary text-[10px] font-bold uppercase tracking-[0.3em]`
3. Main heading: `font-headline font-extrabold text-2xl md:text-3xl tracking-tight text-white` -> `font-headline font-light text-3xl md:text-5xl tracking-[-0.03em] text-on-surface`
4. Provider badge: `bg-purple-500/10 text-purple-400` -> `bg-primary/10 text-primary` (use CSS var)
5. Starter chips: `bg-surface-container-highest text-on-surface-variant hover:text-white hover:bg-surface-bright` -> `bg-white/5 text-on-surface-variant hover:text-on-surface hover:bg-white/10 border border-white/10`
6. User chat bubble: `bg-primary text-on-primary rounded-br-md` -> `bg-primary/80 text-on-primary rounded-br-md`
7. Assistant chat bubble: `bg-surface-container-high text-on-surface rounded-bl-md` -> `bg-white/5 text-on-surface border border-white/10 rounded-bl-md`
8. Brief card: `bg-surface-container-low border border-primary/20 rounded-2xl p-6` -> `glass-card rounded-2xl p-8`
9. Brief card inner grid items: `bg-surface-container-highest rounded-lg p-2` -> `bg-white/5 rounded-lg p-3`
10. Scene list items: `bg-surface-container-highest rounded-lg p-3` -> `bg-white/[0.03] rounded-lg p-3 border border-white/5`
11. Execute button: `bg-gradient-to-br from-primary to-primary-container py-4 rounded-xl text-on-primary font-headline font-black text-lg` -> `bg-gradient-to-br from-primary to-primary-container py-4 rounded-full text-on-primary font-headline font-light tracking-[-0.03em] text-lg shadow-[0_0_30px_rgba(198,191,255,0.2)]`
12. Input bar container: `border-outline-variant/10 bg-surface/80 backdrop-blur-xl` -> `border-white/10 bg-white/5 backdrop-blur-3xl`
13. Textarea: `bg-surface-container-highest border border-outline-variant/20` -> `bg-white/[0.03] border border-white/10`
14. Send button: keep `bg-primary hover:bg-primary-container` (CSS vars handle color)
15. Stop button: keep `bg-error/80 hover:bg-error` (CSS vars handle color)

**=== SPECIFIC TO brief/page.tsx ===**

Apply all universal replacements. Additionally:
1. All card containers: glass-card class
2. All form inputs: bg-white/[0.03] border border-white/10
3. All section headings: font-headline font-light tracking-[-0.03em]
4. All labels: text-[10px] tracking-[0.2em] uppercase font-bold
  </action>
  <acceptance_criteria>
    - intent/page.tsx does NOT contain "#7F72F7"
    - intent/page.tsx does NOT contain "#1A1A2E"
    - intent/page.tsx does NOT contain "#12121A"
    - intent/page.tsx does NOT contain "font-extrabold" (headings converted)
    - intent/page.tsx contains "tracking-[-0.03em]"
    - intent/page.tsx contains "rounded-full" on execute button
    - brief/page.tsx does NOT contain "#7F72F7"
    - brief/page.tsx does NOT contain "#1A1A2E"
    - brief/page.tsx contains "tracking-[-0.03em]"
  </acceptance_criteria>
  <verify>
    <automated>cd "/Users/aasish/Desktop/CLAUDE PROJECTS/NYRADNA/NYRADNA/director" && PASS=true; for f in "src/app/(auth)/create/intent/page.tsx" "src/app/(auth)/create/brief/page.tsx"; do echo "=== $f ==="; if grep -qE "#7F72F7|#1A1A2E|#12121A|#0A0A0F" "$f"; then echo "FAIL: old colors found"; PASS=false; else echo "PASS"; fi; done; $PASS && echo "ALL PASS" || echo "SOME FAILED"</automated>
  </verify>
  <done>intent and brief pages fully converted to cinematic aesthetic. Chat interface uses glass bubbles, starter chips use glass styling, execute button is rounded-full with glow.</done>
</task>

<task type="auto">
  <name>Task 2: Restyle style-dna, asset-sets, and pipeline pages</name>
  <files>src/app/(auth)/create/style-dna/page.tsx, src/app/(auth)/create/asset-sets/page.tsx, src/app/(auth)/create/pipeline/page.tsx</files>
  <read_first>src/app/(auth)/create/style-dna/page.tsx, src/app/(auth)/create/asset-sets/page.tsx, src/app/(auth)/create/pipeline/page.tsx</read_first>
  <action>
Apply the SAME universal color and typography replacements to all three files.

**=== UNIVERSAL COLOR REPLACEMENTS (same table) ===**

| Find | Replace With |
|------|-------------|
| `bg-[#1A1A2E]` | `glass-card` class |
| `bg-[#12121A]` | `bg-white/5` |
| `bg-[#0A0A0F]` | `bg-white/[0.03]` |
| `border-[#7F72F7]/10` | `border-white/10` |
| `border-[#7F72F7]` (solid) | `border-primary` |
| `text-[#7F72F7]` | `text-primary` |
| `text-[#A0A0B8]` | `text-on-surface-variant` |
| `text-[#555]` | `text-outline` |
| `bg-[#7F72F7]` | `bg-primary` |
| `focus:border-[#7F72F7]` | `focus:border-primary/30` |
| `hover:bg-[#7F72F7]/10` | `hover:bg-primary/10` |
| `placeholder-[#555]` | `placeholder-outline` |

**=== TYPOGRAPHY REPLACEMENTS ===**

| Find | Replace With |
|------|-------------|
| `font-headline font-black` | `font-headline font-light tracking-[-0.03em]` |
| `font-headline font-bold` (on headings) | `font-headline font-light tracking-[-0.03em]` |
| `font-headline font-extrabold` | `font-headline font-light tracking-[-0.03em]` |

**=== SPECIFIC TO style-dna/page.tsx ===**

1. All card containers: glass-card
2. All inputs: bg-white/[0.03] border border-white/10
3. Color/style selector cards: bg-white/5 hover:bg-white/10
4. Headings: font-light tracking-[-0.03em]
5. Labels: text-[10px] tracking-[0.2em] uppercase font-bold

**=== SPECIFIC TO asset-sets/page.tsx ===**

1. All card containers: glass-card
2. Heading: font-light tracking-[-0.03em]
3. All hardcoded colors: follow table above
4. Action buttons: ensure border-primary text-primary pattern
5. Empty state: bg-white/[0.02] border-dashed border-white/10

**=== SPECIFIC TO pipeline/page.tsx ===**

This file uses mostly Tailwind semantic colors already. Check and update:
1. `bg-surface-container/80 border border-white/10` -> `glass-card` for preset cards
2. `hover:border-primary/30` -> keep (CSS var handles color)
3. `text-white/80` -> `text-on-surface`
4. `text-white/40` -> `text-on-surface-variant`
5. `text-white/30` -> `text-outline`
6. `text-white/20` -> `text-outline/60`
7. `hover:bg-white/5` -> keep (already glass-friendly)
8. `font-semibold` on headings -> `font-light tracking-[-0.03em]`
  </action>
  <acceptance_criteria>
    - style-dna/page.tsx does NOT contain "#7F72F7"
    - style-dna/page.tsx does NOT contain "#1A1A2E"
    - style-dna/page.tsx contains "tracking-[-0.03em]"
    - asset-sets/page.tsx does NOT contain "#7F72F7"
    - asset-sets/page.tsx does NOT contain "#1A1A2E"
    - asset-sets/page.tsx does NOT contain "#12121A"
    - asset-sets/page.tsx contains "glass-card" OR "bg-white/"
    - pipeline/page.tsx does NOT contain "#7F72F7"
    - pipeline/page.tsx contains "tracking-[-0.03em]" OR "font-light"
  </acceptance_criteria>
  <verify>
    <automated>cd "/Users/aasish/Desktop/CLAUDE PROJECTS/NYRADNA/NYRADNA/director" && PASS=true; for f in "src/app/(auth)/create/style-dna/page.tsx" "src/app/(auth)/create/asset-sets/page.tsx" "src/app/(auth)/create/pipeline/page.tsx"; do echo "=== $f ==="; if grep -qE "#7F72F7|#1A1A2E|#12121A|#0A0A0F" "$f"; then echo "FAIL: old colors found"; PASS=false; else echo "PASS"; fi; done; $PASS && echo "ALL PASS" || echo "SOME FAILED"</automated>
  </verify>
  <done>style-dna, asset-sets, and pipeline pages fully converted to cinematic aesthetic. Zero hardcoded Atlas hex values remain.</done>
</task>

</tasks>

<verification>
- Zero occurrences of #7F72F7, #1A1A2E, #12121A, #0A0A0F in any of the 5 files
- All 5 pages use glass-card or white-alpha backgrounds
- All headings use font-light tracking-[-0.03em]
- `npm run build` completes without errors
</verification>

<success_criteria>
All 5 lighter create pages (intent, brief, style-dna, asset-sets, pipeline) are fully converted to the cinematic luxury aesthetic. Zero hardcoded Atlas hex values remain.
</success_criteria>

<output>
After completion, create `.planning/phases/11-cinematic-redesign/11-04-SUMMARY.md`
</output>
