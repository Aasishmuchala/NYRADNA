---
phase: 11-cinematic-redesign
plan: 02
type: execute
wave: 2
depends_on: [11-01]
files_modified:
  - src/components/layout/Sidebar.tsx
  - src/components/layout/TopNav.tsx
autonomous: true
requirements: [CIN-NAV]

must_haves:
  truths:
    - "Sidebar has glass background (bg-white/5 backdrop-blur-2xl) with right border"
    - "Active sidebar items show border-r-2 border-indigo-400 with purple tint"
    - "Sidebar labels use text-[10px] tracking-[0.2em] uppercase font-bold"
    - "TopNav has glass background (bg-white/10 backdrop-blur-3xl) with bottom border"
    - "Brand name DIRECTOR uses Manrope font-light with negative tracking"
    - "New Project button is rounded-full with glow shadow"
  artifacts:
    - path: "src/components/layout/Sidebar.tsx"
      provides: "Glass sidebar with cinematic styling"
      contains: "backdrop-blur"
    - path: "src/components/layout/TopNav.tsx"
      provides: "Glass top nav with minimal cinematic styling"
      contains: "backdrop-blur-3xl"
  key_links:
    - from: "src/components/layout/Sidebar.tsx"
      to: "src/app/globals.css"
      via: "glass-card class and CSS custom properties"
      pattern: "bg-white/5|backdrop-blur"
    - from: "src/components/layout/TopNav.tsx"
      to: "src/app/globals.css"
      via: "CSS custom properties"
      pattern: "bg-white/10|backdrop-blur-3xl"
---

<objective>
Restyle the navigation shell (Sidebar + TopNav) with the cinematic glass aesthetic. Replace solid backgrounds with glassmorphism, update typography to use wide tracking labels, and apply the cinematic active state indicators.

Purpose: The sidebar and top nav frame every page -- they must set the cinematic luxury tone.
Output: Glass sidebar with right-border active states, glass top nav.
</objective>

<execution_context>
@/Users/aasish/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aasish/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-cinematic-redesign/11-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle Sidebar with glass effect and cinematic typography</name>
  <files>src/components/layout/Sidebar.tsx</files>
  <read_first>src/components/layout/Sidebar.tsx</read_first>
  <action>
Update Sidebar.tsx with the following specific changes:

**1. Aside container** -- replace solid bg with glass + right border:
Old: `bg-surface z-40`
New: `bg-white/5 backdrop-blur-2xl border-r border-white/10 z-40`

**2. Brand section:**
- Logo square: keep `bg-primary` but add `rounded-xl` (was rounded-lg)
- "DIRECTOR" text: change from `font-manrope text-xl font-bold tracking-tight` to `font-headline text-xl font-light tracking-[-0.03em]`
- "AI Video Engine" subtitle: change from `text-[11px] uppercase tracking-widest text-on-surface-variant` to `text-[10px] uppercase tracking-[0.3em] font-bold text-on-surface-variant`

**3. Navigation links -- active state:**
Old active: `bg-surface-container-high text-primary`
New active: `border-r-2 border-indigo-400 bg-gradient-to-r from-indigo-500/10 to-transparent text-primary`

Old inactive: `text-on-surface-variant hover:bg-surface-container-low hover:text-white`
New inactive: `text-on-surface-variant hover:bg-white/5 hover:text-white`

Remove `rounded-lg` from nav links (use full-width style with right border instead). Keep `px-4 py-3` and `transition-colors`.

**4. Nav item text:**
- Change `text-sm font-medium` to `text-sm font-medium tracking-wide`

**5. Workflow section label:**
Keep existing: `uppercase tracking-widest text-[10px] text-on-surface-variant`
Add: `font-bold` (was missing)

**6. Workflow step links -- same active/inactive as nav links above.**

**7. New Project CTA button:**
Old: `rounded-xl bg-gradient-to-br from-primary to-primary-container px-4 py-3 font-bold text-on-primary`
New: `rounded-full bg-gradient-to-br from-primary to-primary-container px-6 py-3 font-bold text-on-primary shadow-[0_0_30px_rgba(198,191,255,0.2)]`

**8. Help & Support section:**
Old border: `border-surface-container-high`
New border: `border-white/10`
Old hover: `hover:bg-surface-container-low`
New hover: `hover:bg-white/5`

**9. Material Symbols icons in sidebar should keep existing size.**
  </action>
  <acceptance_criteria>
    - Sidebar.tsx contains "backdrop-blur-2xl"
    - Sidebar.tsx contains "border-r border-white/10"
    - Sidebar.tsx contains "border-r-2 border-indigo-400" (active state)
    - Sidebar.tsx contains "from-indigo-500/10" (active bg gradient)
    - Sidebar.tsx contains "tracking-[-0.03em]" (brand name)
    - Sidebar.tsx contains "tracking-[0.3em]" (subtitle)
    - Sidebar.tsx contains "rounded-full" (CTA button)
    - Sidebar.tsx contains "shadow-[0_0_30px_rgba(198,191,255,0.2)]"
    - Sidebar.tsx does NOT contain "bg-surface z-40" as sidebar background
    - Sidebar.tsx does NOT contain "bg-surface-container-high text-primary" as active state
  </acceptance_criteria>
  <verify>
    <automated>cd "/Users/aasish/Desktop/CLAUDE PROJECTS/NYRADNA/NYRADNA/director" && grep -q "backdrop-blur-2xl" src/components/layout/Sidebar.tsx && grep -q "border-r-2 border-indigo-400" src/components/layout/Sidebar.tsx && grep -q "tracking-\[-0.03em\]" src/components/layout/Sidebar.tsx && grep -q "rounded-full" src/components/layout/Sidebar.tsx && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Sidebar uses glass background with border-r, active items have border-r-2 border-indigo-400 gradient, brand uses font-light tracking-[-0.03em], CTA is rounded-full with glow shadow.</done>
</task>

<task type="auto">
  <name>Task 2: Restyle TopNav with glass effect and minimal cinematic styling</name>
  <files>src/components/layout/TopNav.tsx</files>
  <read_first>src/components/layout/TopNav.tsx</read_first>
  <action>
Update TopNav.tsx with the following specific changes:

**1. Nav container:**
Old: `bg-surface/70 backdrop-blur-xl border-b border-surface-container-high`
New: `bg-white/10 backdrop-blur-3xl border-b border-white/10`

**2. Search input:**
Old: `bg-surface-container px-4 py-2 text-sm text-white placeholder-on-surface-variant ring-1 ring-surface-container-high ... focus:ring-primary/50`
New: `bg-white/5 backdrop-blur-xl px-4 py-2 text-sm text-on-surface placeholder-on-surface-variant border border-white/10 rounded-full ... focus:ring-1 focus:ring-primary/30 focus:border-primary/30`
(Change from ring-1 to border, make rounded-full instead of rounded-full which it already is)

**3. Notification bell button:**
Old hover: `hover:bg-surface-container-high`
New hover: `hover:bg-white/10`

**4. Help icon button:**
Old hover: `hover:bg-surface-container-high`
New hover: `hover:bg-white/10`

**5. Notification indicator dot:**
Keep `bg-primary` (it will now be #c6bfff via CSS var)

**6. User avatar button:**
Old: `bg-surface-container-high ring-1 ring-surface-bright`
New: `bg-white/10 ring-1 ring-white/20`

**7. User name text:** Keep `text-sm font-medium text-white` but change to `text-sm font-medium text-on-surface`

**8. User plan text:** Keep `text-xs text-on-surface-variant`

**9. Focus rings:** Update all `focus:ring-primary/30` -- keep as-is (will resolve to new #c6bfff)
  </action>
  <acceptance_criteria>
    - TopNav.tsx contains "bg-white/10 backdrop-blur-3xl"
    - TopNav.tsx contains "border-b border-white/10"
    - TopNav.tsx contains "bg-white/5" (search input)
    - TopNav.tsx contains "hover:bg-white/10" (button hovers)
    - TopNav.tsx contains "ring-white/20" (avatar ring)
    - TopNav.tsx does NOT contain "bg-surface/70 backdrop-blur-xl"
    - TopNav.tsx does NOT contain "bg-surface-container-high" (old hover/bg)
  </acceptance_criteria>
  <verify>
    <automated>cd "/Users/aasish/Desktop/CLAUDE PROJECTS/NYRADNA/NYRADNA/director" && grep -q "backdrop-blur-3xl" src/components/layout/TopNav.tsx && grep -q "bg-white/10" src/components/layout/TopNav.tsx && grep -q "border-white/10" src/components/layout/TopNav.tsx && ! grep -q "bg-surface/70" src/components/layout/TopNav.tsx && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>TopNav uses glass background (bg-white/10 backdrop-blur-3xl) with border-white/10 bottom border. All interactive elements use white-alpha hover states. Search input uses glass styling.</done>
</task>

</tasks>

<verification>
- Sidebar renders with frosted glass effect and lavender-tinted right border
- Active sidebar item shows right-2 indigo border with gradient tint
- TopNav renders with frosted glass effect across full width
- No solid dark backgrounds remain in either component
- `npm run build` completes without errors
</verification>

<success_criteria>
Both navigation components use glassmorphism with white-alpha backgrounds and backdrop-blur. Active states use border-r-2 indigo indicator. All hardcoded Atlas hex values removed. Typography uses cinematic tracking.
</success_criteria>

<output>
After completion, create `.planning/phases/11-cinematic-redesign/11-02-SUMMARY.md`
</output>
