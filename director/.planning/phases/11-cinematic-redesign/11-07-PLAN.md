---
phase: 11-cinematic-redesign
plan: 07
type: execute
wave: 5
depends_on: [11-03, 11-04, 11-05, 11-06]
files_modified: []
autonomous: false
requirements: [CIN-AUDIT]

must_haves:
  truths:
    - "Zero occurrences of #7F72F7 in any .tsx or .css file"
    - "Zero occurrences of #A78BFA in any .tsx or .css file"
    - "Zero occurrences of #0A0A0F in any .tsx file (except globals.css #0a0a0e)"
    - "Zero occurrences of #12121A in any .tsx file"
    - "Zero occurrences of #1A1A2E in any .tsx file"
    - "Zero occurrences of #A0A0B8 in any .tsx file"
    - "Manrope font loads in the browser"
    - "App builds without errors"
  artifacts: []
  key_links: []
---

<objective>
Run a full codebase audit to verify the cinematic redesign is complete. Grep for all old Atlas hex values, verify the build succeeds, and do a visual spot-check.

Purpose: Catch any files that were missed or have lingering old values.
Output: Clean bill of health or targeted fix list.
</objective>

<execution_context>
@/Users/aasish/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aasish/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-cinematic-redesign/11-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full codebase audit for old Atlas hex values and build verification</name>
  <files></files>
  <action>
Run the following audit commands and fix any remaining issues:

1. **Grep for ALL old Atlas hex values across the entire src/ directory:**
```bash
grep -rn "#7F72F7\|#A78BFA\|#0A0A0F\|#12121A\|#1A1A2E\|#A0A0B8\|#6B6B80" src/ --include="*.tsx" --include="*.css"
```

If ANY matches found: fix them using the color mapping from CONTEXT.md:
- #7F72F7 -> #c6bfff (primary) or text-primary CSS class
- #A78BFA -> #c6bfff (primary) or text-primary CSS class
- #0A0A0F -> #050507 (body) or bg-surface CSS class, or bg-white/[0.03] for inputs
- #12121A -> bg-white/5 or bg-white/[0.03]
- #1A1A2E -> glass-card class or bg-white/5
- #A0A0B8 -> #c8c4d6 or text-on-surface-variant CSS class
- #6B6B80 -> #928f9f or text-outline CSS class

2. **Also grep for #555 as a standalone hex color (not inside other hex values):**
```bash
grep -rn "text-\[#555\]\|bg-\[#555\]\|color.*#555\|'#555'" src/ --include="*.tsx" --include="*.css"
```
Replace any found with text-outline or #928f9f.

3. **Verify font-black/font-extrabold are not on headlines:**
```bash
grep -rn "font-headline font-black\|font-headline font-extrabold\|font-headline font-bold" src/ --include="*.tsx"
```
Note: `font-headline font-bold` is acceptable ONLY on small labels/badges, NOT on h1/h2/h3 headings. For headings, it should be `font-headline font-light tracking-[-0.03em]`. Check context of each match.

4. **Verify build succeeds:**
```bash
cd "/Users/aasish/Desktop/CLAUDE PROJECTS/NYRADNA/NYRADNA/director" && npm run build
```

5. **If any old hex values or build errors found, fix them in place.** Each fix should follow the exact same patterns used in plans 01-06.
  </action>
  <verify>
    <automated>cd "/Users/aasish/Desktop/CLAUDE PROJECTS/NYRADNA/NYRADNA/director" && echo "=== Old hex values ===" && grep -rn "#7F72F7\|#A78BFA\|#0A0A0F\|#12121A\|#1A1A2E\|#A0A0B8\|#6B6B80" src/ --include="*.tsx" --include="*.css" | grep -v "node_modules" | wc -l && echo "=== Build ===" && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Zero occurrences of any old Atlas hex value across the entire src/ directory. Build succeeds without errors.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete cinematic luxury redesign of the entire NYRADNA Director app. Replaced Atlas Cloud purple (#7F72F7) theme with soft lavender (#c6bfff/#8c80ff) cinematic palette. Glassmorphism throughout, Manrope headlines with font-light, spacious layouts, thin Material Symbols icons.</what-built>
  <how-to-verify>
1. Run `npm run dev` and open http://localhost:3000
2. **Landing page**: Verify heading uses thin font weight (not bold), CTA buttons are rounded-full with glow, background has subtle purple/pink ambient blobs
3. **Login/navigate to dashboard**: Verify sidebar has glass effect (slightly translucent), active item has right-side indigo border indicator
4. **TopNav**: Verify glass effect with translucent background
5. **Navigate to /create/intent**: Verify chat interface has glass-styled bubbles, starter chips have glass bg
6. **Navigate to /create/directors-cut**: Verify form cards are glass panels (translucent with lavender border), headings use thin font weight
7. **Navigate to /create/pipeline**: Verify pipeline nodes have glass backgrounds
8. **Check overall feel**: Should feel like a high-end cinematic creative suite -- sparse, luxurious, spacious
  </how-to-verify>
  <resume-signal>Type "approved" or describe specific issues to fix</resume-signal>
</task>

</tasks>

<verification>
- Automated: Zero matches for old hex values across src/
- Automated: npm run build succeeds
- Human: Visual verification of cinematic luxury aesthetic
</verification>

<success_criteria>
Complete cinematic redesign verified by both automated audit (zero old hex values, clean build) and human visual inspection (glass effects visible, thin typography, spacious layouts, lavender palette).
</success_criteria>

<output>
After completion, create `.planning/phases/11-cinematic-redesign/11-07-SUMMARY.md`
</output>
